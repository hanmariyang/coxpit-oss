# Coxpit Design System

The board is a single self-contained page (`src/board.ts`), so the design system lives as CSS custom properties plus the component classes below. **Every new piece of UI must be built from these — no native browser UI** (no `alert`/`confirm`/`prompt`, no bare `<select>`, no default buttons).

## Identity

A dark fleet console: an instrument you *operate*, not a document you read. Data is monospaced, chrome is quiet, one teal accent means "coxpit is acting". Semantic status colors are separate from the accent and never used decoratively.

## Tokens (`:root` in board.ts)

| Token | Value | Use |
|---|---|---|
| `--bg` | `#0b0d12` | page ground |
| `--surface` / `--surface2` | `#12151c` / `#171b24` | cards, panels / nested panels, chips |
| `--line` / `--line-hi` | `#222835` / `#2f3648` | borders / hover borders |
| `--ink` / `--muted` / `--faint` | `#dee4ec` / `#8792a2` / `#5c6675` | text hierarchy (3 steps, no more) |
| `--brand` / `--brand-ink` / `--brand-dim` | `#4ec9b0` / `#062822` / 13% teal | the one accent; ink-on-brand; washes |
| `--s-*` | pending gray · preparing amber · running blue · done green · failed/error red · stopped violet · merged teal | status only — never decoration |
| `--mono` / `--sans` | ui-monospace stack / system stack | data vs UI text |
| `--r-card` / `--r-ctl` | 10px / 8px | cards / controls |
| `--shadow` | `0 8px 28px rgba(0,0,0,.35)` | floating layers only |

Type: UI text 13–14px sans; data (ids, branches, logs, diffs, versions) always mono; numbers in tables get `tabular-nums`; section labels are 10–11px mono uppercase with wide tracking.

## Components

| Component | Class | Rules |
|---|---|---|
| Buttons | `.btn` `.btn-ghost` `.btn-danger` (+`.sm`) | one solid teal primary per view; ghost for secondary; danger is outline red, reserved for destructive |
| Status chip | `.chip` (+`.running`) | dot + uppercase mono label, colored by `--s-*`; running pulses (respects reduced-motion) |
| Cards | `.card` | hover = border lighten + 1px lift; whole card is the click target |
| Inputs | `input, textarea` | `#0e1118` well, teal focus border; placeholders in `--faint` |
| Dropdown | `.dd` (wraps a hidden `<select>`) | `dressSelect(id)` enhances; native select stays in DOM as the state holder so `$(id).value` contracts hold; ✓ marks current; Esc/outside closes. The sheet selects (`#repoMachine`/`#taskRepo`/`#taskCapture`) are all dressed so their **popup is a dark `.dd-panel`, never the OS list**. Any still-bare `<select>` (e.g. `#archRepo`) also gets `appearance:none` + a token chevron background so no white native arrow/popup ever leaks |
| Segmented | `.seg` / `.seg-opt` | binary/ternary mode choices; **slim single row** (`.seg-opt` is `inline-flex` row — never a `flex-direction:column` stack, which reads boxy). The risky option gets brand fill; its mono `spends credits` hint (`.seg-hint`) rides **inline and only appears once the option is selected** so the resting seg stays clean and matches the type/provider seg height |
| Count stepper | `.stepper` (`#taskCountStep`) around `input[type=number]` | native number spinners render white — killed with `input[type=number]{appearance:none}` + hidden `::-webkit-*-spin-button`; replaced by token `−`/`+` `.stp` buttons (`#cntDown`/`#cntUp`, ghost feel, `#i-minus`/`#i-plus`) flanking a centered `tabular-nums` field, clamped 1..8 (`stepCount`, manual typing re-clamps on `change`). The `<input>` stays the state holder (`$('taskCount').value`) |
| Toast | `toast(msg, 'ok'\|'error')` | top-right stack, auto-dismiss ~4s; for outcomes and errors — never questions |
| Confirm | `confirmUI(msg, {sub, danger, okLabel})` | promise-based dialog; every destructive action goes through it with a consequence line in `sub` |
| Modal | `.overlay` + `.modal` (+`.wide`, `.term`) | backdrop blur, Esc/backdrop closes; header = id · title · chip · × |
| Empty state | onboarding `.setup` panel | never a bare "no data" — show machine readiness + next steps |
| Field label | `.flabel` | 10px mono uppercase over every form control — placeholder-only fields are not allowed |
| Repo browser | `.brw` modal + `GET /api/browse` | pick-don't-type: navigate folders, git repos get a badge + inline Register; local machine only (remote = manual path) |
| Export dialog | `#expOverlay` (`.cfm` variant) | one input + consequence line; empty dest falls back to `~/coxpit-exports/r<id>` |
| PR affordance | `Open PR` ghost in compare · `PR` + `#i-external-link` link on cards | once a run has a PR, the button becomes the link — never both |
| Select mode | `Select runs` toolbar toggle + `.selbar` | cards grow a ✓ circle; click toggles instead of opening; the floating bar states the consequence ("merges in selection order · conflicts spawn an integration agent") |
| Plan form | `#planForm` sidebar section | one goal → planner splits → auto-launch; button narrates its long wait ("Planning… (1–3 min)"); follows the global Dry/Real mode |
| AI review panel | `#cmpReview` above compare columns | reviewer digests every diff into approach/pros/cons/recommendation (mdLite-rendered) — the human judges, not reads | — full-height scrollable pane; the AI-review button toggles it in place of the diff columns
| Timeline lines | `humanize()` | never raw JSON: `said`/`tool ▸ Name — file`/`done`/`steer`/`ask ?`/`sync`; rate-limit and tool-result echoes are dropped as noise. The modal always refetches the full timeline on open (`GET /api/runs/:id`) — the fleet payload caps events at 40/run |
| Session bar | Work/Ask mini-seg + input in run modal | the run IS a session: Work = next instruction, Ask = question only (no file changes); Sync base refreshes a long-lived worktree |
| Notify bell | `#bell` header toggle (`#i-bell-off`/`#i-bell`) | browser notification on run settle; server-side twin is `COXPIT_WEBHOOK_URL` (JSON `run.settled` POST) |
| Navigator rail (v5.0) | `aside.rail` = machine switcher → Repositories(`#repoList`) → View(`#viewNav`) → spacer → `#newBtn` → Library | the rail **navigates**, it does not compose: a compact machine button, a scannable repo list with live counts + scoping, the moved view nav, and a single `＋ New` that opens the launcher. Never put a permanent compose form back in the rail — launching is a focused action behind `＋ New`. (Supersedes the v4.x Context→Start→Library layering) |
| Machine switcher | `.machine` button (`#machineSwitch`, `#i-server` + `.mdot2` reachability) + `.mmenu` menu | replaces the machine `<select>`; shows the current machine + an online dot; click opens a small anchored menu of machines; the hidden `#repoMachine` `<select>` stays as the state holder so every `machineSlug` contract still reads `$('repoMachine').value` |
| Repo row | `#repoList .repo` (`#i-folder` · name · `.cnt` active-run badge · `.attn` dot) | one row per registered repo; **counts are computed client-side** by grouping the loaded `/api/fleet` active runs by `tasks.get(run.taskId).repoId` (no endpoint); a failed/error run raises the red `.attn` dot; the selected row is `.repo.on` (persisted `localStorage('coxpit.repo')`). Row hover reveals a `.rowmenu` with per-repo **base-branch ⎇** and **remove ×** (`.rmbtn`, reusing `openBranchFor`/`removeRepoById`) styled as **icon-ghost** (transparent bg · `--muted` `#i-branch`/`#i-x` icon · hover → `--ink` + `--surface` fill + `--line-hi` border) — never a white button. The in-sheet repo-management row (`#repoActions`: Browse/New/Path/⎇/×) uses the same `.btn-ghost` dark treatment. An **All repositories** pseudo-row (`data-repo="all"`, `#i-layers`) clears the scope. Empty state = an "Add your first repository" CTA. Scoped under `#repoList` so the Library capture `.repo` rows are untouched |
| View nav | `#viewNav` `.navi` (Active `#i-layers` / Goals `#i-target` / Archive `#i-archive`), each with a mono count | moved out of the header (the old `#viewSeg` is gone): **Active** = active runs flat (repo-scoped), **Goals** = the group bands only (client-side filter to grouped tasks, rendered as the existing `.gband`s), **Archive** = the `/api/archive` list (repo-scoped via `?repo=`). Reuses the existing `setView`/`render` logic — only the entry point moved. `.navi.on` = `--surface2` fill + brand icon |
| New sheet (v5.0 Part B) | `#newBtn` → `.overlay#newSheet` → `.sheet` card: header (`＋ New · <selected repo>` from `#sheetRepoLbl` + × `#sheetClose`) → **type seg** `#launchTabs` (Task│Goal│Workbench) → `#taskForm` (`.sheet-body` scroll region + `.sheet-f` footer). The launch **logic is relocated, not rewritten**: the same `#taskForm` submit handler branches on `lTab`, and the hidden `#taskRepo`/`#repoMachine` `<select>`s still hold state | a focused compose sheet, not a permanent rail form. `setV(tab)` swaps the body panel **and adapts the footer**: **Task** = provider seg + progressive **Options** reveal → Dry/Real · count `#taskCount` · **Run fleet**; **Goal** = `#planGoal` + provider seg → Dry/Real · **Plan & run** (no count — the planner decides N); **Workbench** = optional name + explainer → **Open workbench** only (mode seg + count hidden). Target repo = the rail-scoped repo; `＋ New` opens on **Task**. Closing resets the repo picker affordances |
| Options reveal | `.opt` disclosure in the Task body (`#taskOptToggle` `aria-expanded` + `#taskOptBody`, `.opt-car` chevron rotates) | the rarely-used Task fields — **model · design capture · deliverables** — collapse under a single "Options" row (subtitle `model · design · deliverables`); collapsed by default, expanded state remembered per session (`sessionStorage('coxpit.taskOpt')`). Keeps the common path (title · prompt · provider · run) to one glance |
| Repo picker (in-sheet) | `#repoActions` row (Browse/New/Path/⎇/×) + `#repoForm`, both `hidden` by default | reached from the rail **Add repository…** (`#repoAdd`) which opens the sheet and unhides the row → the existing Browse modal (New-folder/Path inside). Per-repo ⎇/× also live on the rail repo-row hover menu (Phase 1); these are the sheet-side fallback wired to the same `openBranchFor`/`removeRepoById` |
| Workbench | launcher tab + `--s-open` status | worktree+tmux, no agent: humans (and their interactive claude) work inside; the card keeps the diff/merge/PR/export rails |
| Terminal | full-screen view, xterm themed with tokens | not a modal — `#termOverlay` fills the viewport (padding 0, no radius); background `--bg`, cursor `--brand`; CJK-safe font stack + unicode11 widths; Esc or × closes |
| Terminal tabs | `.term-tabs` / `.ttab` in the terminal header | every live session (`running`·`open`) is a tab — switch sessions without leaving the terminal; active tab = `--brand` border; re-rendered on every board update |
| Provider seg | `#provSeg` in the Task panel | Claude/Codex per launch (persisted in localStorage); Task tab only — the Goal planner and Workbench are provider-neutral; cards already carry the agent name as the badge |
| Pocket board (v5.0 Part C) | `.menu-btn` (`#menuBtn`) → `aside.open` off-canvas drawer + `.scrim` + FAB (`#fab`), all ≤860px | the fleet is the first screen. The drawer is **just the navigator** (machine switcher · repo list · view nav) — the compose form is no longer crammed into it. A floating **`＋` FAB** (`.fab`, mono brand disc, Lucide `#i-plus`) opens the same `#newSheet` compose sheet (full-bleed on mobile via the existing mobile-modal CSS) and closes the drawer; the rail's inline `#newBtn`/`.newnote` are hidden on mobile (`#newBtn,.newnote{display:none}`) so there's one launch affordance. FAB is desktop-hidden (`.fab{display:none}` base → `inline-flex` only in the ≤860px query). Reuses the v3.6 drawer plumbing (`setDrawer`, `translateX(-103%)` off-canvas + `aside.open{translateX(0)}`) |
| Mobile stability | root guard in the base `:root`/`html`/`body` block | kills the iOS left-right rubber-band + horizontal pan so the PWA feels native: **`html{overscroll-behavior:none}`** (stops the rubber-band) + **`body{overflow-x:hidden;overscroll-behavior-x:none}`** (clamps horizontal overflow). **Never `overflow` on `<html>`** — that breaks iOS `position:fixed`/sticky, which the fixed header and the off-canvas drawer both depend on; clamp **body only**. The off-canvas drawer (`aside` `translateX` off-screen, `overflow-x:hidden`, `max-width:100vw`) is clipped by the body clamp so it never joins the scrollable width. Wide content (diffs/code/tables) still scrolls inside its own `overflow-x:auto` container; `viewport-fit=cover` + `env(safe-area-inset-*)` respected (FAB/term-ibar offsets) |
| Mobile modals | full-bleed at ≤860px | run modal stacks Timeline over Diff (1fr/1fr rows); compare columns stack vertically; footers wrap; inputs go 16px (iOS focus-zoom guard) |
| Deep link | `/?run=N` + notification `onclick` | any surface (webhook message, browser notification) can land directly on a run modal; the param is consumed then stripped from the URL |
| Doc mode | `#cmpDocsTgl` → `docsHTML()` (compare only) | Rendered ↔ Diff toggle in the compare panel; changed `.md` renders via mdLite, `.html` in a fully sandboxed iframe (no scripts). The run modal's own toggle is **subsumed by the v4.7 output cards** — diff is the `code` card, rendered docs/pages are `doc`/`page` cards |
| Contract strip | `.contract` / `.req` (run modal, under the header) | `Required outputs (contract): [icon type]… — Extra: …` from `GET /api/runs/:id/outputs`; `#i-check` present (green), `#i-alert-triangle` declared-but-missing (amber `.req.warn`), Extra (incidental) is dashed `.req.aux`; hidden when a run declares nothing and produced no cards |
| Output cards | `#outCards` + `.ocard` (run modal right column) | one card per deliverable — a typed Lucide icon (`OUT_ICON`: answer→`#i-message` · code→`#i-code` · doc→`#i-file` · page/file→`#i-image`) + title + `Required`/`Extra` badge + meta + `#i-chevron`; missing declared cards render dashed (`.miss`, "output not met") and are not clickable; `pickDefaultCard()` auto-opens the run's primary deliverable (doc/answer when no code, else the declared doc, else code, else first present) |
| Output viewer | `#outDetail` + `.oback` (swaps in over the cards) | per-type real render: `answer`/`doc` → mdLite (`.doc-md`); `page` → live `sandbox=""` `srcdoc` iframe (`.doc-frame`); `code` → the colored `<pre class=diff>` renderer with `.dl-line` steer clicks; `file` image → `<img class=oimg src=/api/runs/:id/file>` (escA'd path), other file → `.odl` download link (+ small text → mono `<pre>`); `‹ 산출물 목록` returns to the list |
| Diff comments | `.dl-line` (run modal `code` card viewer) | every +/- line is a click target that quotes `file: "line" — ` into the steer input; hover = brand tint; hidden runs (not settled) ignore clicks |
| Deliverables selector | `#taskOutputs` + `.ochip` (Task compose, inside the Options reveal) | optional contract — chips `Answer·Code·Doc·Page·File` toggle the declared `tasks.outputs`; default none = today's behavior (no contract, run shows whatever it produced); selecting any sets the contract + prompt injection; on = `.brand-dim` fill |
| GitHub import | `#ghOverlay` (`.cfm` variant) + `#ghImport` in Task panel | fetch drafts the form — never auto-launches; the human reviews, picks a provider, runs |
| Share affordance | `Share` ghost in run modal footer | mints/reuses one read-only link per run, copies to clipboard; the share page is server-rendered, script-free, action-free |
| Swarm lineage | `↳ by rN` card meta + `swarm` timeline line | subtask cards carry their parent; the parent's timeline logs each spawn (`↳ spawned task #N`) |
| Terminal input bar | `.term-ibar` + `.tkey` (touch/≤860px) | direct xterm typing breaks IME composition on software keyboards (jamo arrive decomposed) — the bar is a real input that composes natively and sends whole lines; esc·tab·^C·↑·↓ chord keys; safe-area padded |
| Doc provenance | `.doc-src` line above the rendered docs | when the docs come from a snapshot (worktree gone after merge/close), a faint mono line says so — the reader knows they're seeing preserved output, not a live read |
| Model input | `.mcombo` (`#modelCombo`) = `#taskModel` free-text input + a `.dd`-style recent-models menu (`#modelMenu`, caret `#modelTog`) | free text stays (CLI model names churn — no hardcoded list), empty = CLI default. The old native `<datalist>` popup rendered white and un-styleable → **replaced by a dark `.dd-panel` menu** of the last-5 (localStorage); picking one fills the input, typing is unchanged. Esc/outside closes; the card meta shows `⚙ model` when set |
| Base branch dialog | `#brOverlay` (`.cfm` variant), reached from the repo-row hover `⎇` (v5.0) or the New-launcher `#repoBranch` | merge · Sync base · PR all target it; validated against the repo (must exist); `openBranchFor(id)` captures the target repo id so it works from either entry point |
| New project | `#npOverlay` (`.cfm` variant) + `New` in the New launcher's repo-management row | the only place coxpit ever runs `git init`; empty/missing/commitless paths only — a populated folder is never initialized; success auto-selects the repo |
| Remote access | onboarding section (`#rmtOnboard`) + `#remoteOverlay` (`.cfm` variant) via header `#i-external-link` | detect Tailscale; one-click **Serve** (tailnet, safe, default) shows/copies the HTTPS `.ts.net` URL; **Funnel** (public) is the risky red toggle, disabled unless auth is set; non-Tailscale users get copy-paste recipes (`.rmt-more` details) with the real `config.port`. coxpit drives the tool, never hosts a relay |
| Commitless register | `NO_COMMITS` 400 → confirm offers greenfield | register never silently stores a broken defaultBranch; the confirm (Register form + Browse Register) reuses `#npOverlay`'s POST to seed an empty initial commit |
| Close guard | danger `confirmUI` on 409 from close | closing deletes worktrees — if any run has unmerged, unexported, non-PR changes the server 409s with `atRisk`; the dialog lists them (`rN · M files`) and only `force:true` proceeds |
| Closed card | `.card.closed` (dim + hatched `.log` + CLOSED stamp) | a closed card must read as closed at a glance — desaturated, diagonal hatching over the timeline, a rotated CLOSED stamp; the status chip stays (done/merged history) |
| Group band | `.gband` (`grid-column:1/-1`) around sibling cards | goal (`⌁`) / swarm (`↳`) siblings cluster under a header (title · N tasks · M settled) with fold/Select runs/Close group — a **dashed border**, never a left accent bar or a color fill; cards inside stay first-class, single-run tasks never band |
| Attempt counter | `.attempt` in card meta | any task with >1 run shows `run i/n` so same-title cards read as attempts, not duplicates; brand-tinted, mono |
| Group actions | band header buttons (delegated on `#grid`) | Select runs reuses the existing select mode + selbar (no new merge path); Close group loops the same close endpoint honoring the v4.1 guard, aggregating every `atRisk` into one confirm; fold state persists in localStorage across WS repaints |
| View seg (retired) | ~~`#viewSeg` in header~~ → moved to the rail `#viewNav` (v5.0) | the Active/Archive header seg was **removed** in v5.0 and re-homed in the rail as `#viewNav` (with a new **Goals** entry); the closed-count badge now rides the Archive nav row (`#navArchiveN`). Archive is still a static snapshot (WS updates ignored while shown) |
| Reclaim worktrees | `#reclaimBtn` (`#i-recycle` Reclaim · N · ~XMB) in the Archive `.arch-bar` | a maintenance affordance that reclaims disk debt — run worktrees left behind by cleaned/failed runs (~180MB each, node_modules lives inside). Fed by `GET /api/worktrees` (`{items,totalKb}`); **hidden when nothing is reclaimable**. Click → danger `confirmUI` ("remove N cleaned/failed run worktrees? active work is untouched") → `POST /api/worktrees/prune` → toast the reclaimed count + refresh. Safety is server-side: the endpoint only lists worktrees for **closed** tasks or **failed/error/stopped** runs — running/preparing/`done` (active or successful-possibly-unmerged) are never eligible or removed |
| Archive rows | `.arch-row` in `#archive` | closed tasks as compact single-line rows (title · run summary · repo · date), newest first, with a debounced title search and repo filter + load-more paging; a row opens the run modal via `GET /api/tasks/:id` — diff says worktree gone, Rendered falls back to the v4.1 snapshot |
| Converge cockpit | `.runs` + `.run` decision rows (workroom Work body) | the Goal workroom's Work mode is a **per-run decision panel**, not a merged timeline: one `.run` row per group run — collapsed head = checkbox · status dot · `rN` · title · meta · inline `[Review][Steer][Merge][Close]` · caret; merged rows dim + `merged → base` badge (action → `Open`), running rows lock (`Open · decide once settled`). Expanding a row loads the run's **P2 output cards** (`roomLoadRunOutputs` → `outCardHTML` + `renderOutCardInto`, no duplicate renderer) as the review peek + an inline steer input (`Steer → continue session`); `[Review]` runs `reviewTask` and shows the `#i-message` summary inline. All actions reuse existing endpoints (`/runs/:id/merge·steer`, `/tasks/:id/review·close`) — no new decision logic; Ask mode is unchanged |
| Group action bar | `.gbar` (workroom, above the runs) | `☑ N selected · Integrate selected (N)` (brand, reuses select-mode + `/api/integrate`) · spacer · `Review all` (`reviewTask` loop) · `Close group` (danger, reuses `closeGroup` guard); Work-mode only (hidden in Ask) |
| Terminal guard | `#mTerm` disabled + tooltip (`termUnavailReason`) | a run whose task is closed or whose worktree was cleaned (merged/cleanup → empty `worktreePath`) has no live tmux session — the Terminal button is disabled with a "worktree cleaned — terminal unavailable" tooltip. Server twin: `cleanupRun` blanks `worktreePath`/`tmuxWindow` so `getRunTermInfo` returns null and `/ws/term/:id` closes with a clear reason instead of attaching a dead session |
| Login / setup page | standalone HTML (`src/login.ts`), served by `authGate` on an unauthenticated **HTML GET** (200, never a Basic popup) | the branded replacement for the browser's native auth dialog — a single centered `.card` on `--bg`, the mono **coxpit** mark + a lock/unlock glyph, **no username** (key-only, single-owner). **Unlock**: one `access key` password field + a "Remember this device" checkbox (30-day cookie vs session) + a mono `.err` rate-limit line ("wrong key — N attempt(s) left" / "try again in Ns"). **Setup** (first-run, no key configured): adds a `confirm access key` field and a `setup token` field with an explanatory `.hint` (paste the one-time token from the daemon log unless you're on `http://127.0.0.1` directly — anti-claim). A footer link points at the remote-access docs. **Submit = a real form navigation POST** (v5.0 Part C — `<form method="post" action="/api/auth/{unlock,setup}">` with a hidden `nav=1` flag, urlencoded): the server answers **303 → `/`** on success so the **browser commits the `Set-Cookie` on the navigation response** before landing on the board — this fixes mobile Safari, which dropped the fetch-response cookie before the old `location.replace('/')` and bounced back to login. Failure re-renders this same page server-side with the error inline (`loginPageHTML(setup,{error})`), so it works with JS off; client JS only pre-validates (empty key, confirm-match, min-length) and disables the button. The **JSON API is unchanged** (`content-type: application/json`, no `nav`) → still `{ok}`/201/401 for automation. Key inputs carry `autocapitalize="none" autocorrect="off" spellcheck="false"` and go 16px (iOS focus-zoom guard); the mono `.err` line surfaces the rate-limit block ("too many attempts — try again in Ns"). Auth is **exposure-conditional**: a loopback-only bind (`127.0.0.1`) is trusted-local and never shows this page; only an exposed bind (`0.0.0.0`/routable) or `COXPIT_AUTH_PASS` on an exposed host gates. Uses board tokens verbatim; the lock/unlock marks are the Part-B Lucide icons (`#i-lock`/`#i-unlock`) |
| Work tree (v6.0 T1) | cockpit `#tree` — `.lbl` **Sessions** section first, then one section per repo: `.tnode.repo` → `.tnode.goal` band → `.tnode.task` (the work) → `.tnode.run` (the session) | the tree *is* the hierarchy Project ▸ Work ▸ Session; names live on the work, roles on the run. A run row reads `role · model` (e.g. `구현 · opus`) with the status dot on the left and a faint mono `rN` in `.meta` — the id never disappears, it just stops being the name. A group/goal renders as a **thin dashed band** inside its repo section (same rule as the board's `.gband`: dashed border, never a left accent bar). Machine scoping unchanged; the same render path fills the ≤860px drawer |
| Tree node action | `.tact` on a `.tnode` (`data-newwork` on the repo row · `data-workmd`/`data-newagent` on the work row) | the affordances that *act* from the tree: `＋ 새 작업` (repo), `▤ WORK.md` and `＋ 에이전트` (work). Icon-ghost mono text — transparent, `--faint`, revealed on row hover (`.7` opacity on touch, where hover does not exist), hover → `--brand` on a `--surface2` fill. Never a filled button: the tree navigates, and one row must not out-shout the rest. Two actions on one row: only the **first** takes the `margin-left:auto` (`.tact+.tact` resets it) — two auto margins would split the free space and drift apart |
| Work context (v6.0 W2) | `▤ WORK.md` `.tact` on the work row → the **existing file-viewer pane** (`ensureViewer(path,name,{edit:true})`) on `<dataDir>/work/<taskId>.md` | one markdown file per work — goal, decisions, constraints — injected into every agent launched or steered under it. **A mono `▤`, never an emoji** (the cockpit's glyph rule; the spec's 📝 is shorthand). No new file surface: the doc lives under `~/.coxpit`, which is inside the viewer's default root, so `/api/fs/read`·`/api/fs/write` already reach it; `POST /api/tasks/:id/work` only creates the empty file on first open and says whether that root covers it (`inRoot:false` → the same pane reads/writes through `GET`/`PUT /api/tasks/:id/work` instead — the affordance bends, it does not break). `md` viewer panes gained the same `편집` button the text panes already had, so preview ↔ edit happens in place. The row's `title` states the contract plainly — **saved edits reach the next launch/steer, not a running turn** — because a context file that silently misses the turn you are watching is worse than none |
| New-work sheet | `.modal#workModal` — the `.pick` shell (`.pick-h` · `.pick-path` repo line · `.sheet-note` · `.pick-f` with `.pick-name` + `.go`) | reuses the session-name input pattern verbatim, so naming a work feels like naming a session. Creating it opens a **`main`** terminal at the repo checkout (D2) — the note says so in one line, because "새 작업" that silently makes a worktree would be a lie. Enter submits; backdrop/× closes |
| Add-agent sheet | `.modal#agentModal` — `.pick` shell + `.sheet-body` (`.flabel` over each control: role `.pick-name` · provider `.modes`/`.mode` seg from `/api/fleet` providers · model `.pick-name` · **place `.modes`/`.mode` seg `#agentPlace`** + `.sheet-tradeoff` + `.sheet-err`) + `.pick-f` (`.rchk` real · `#agentPlaceHint` · `.go`) | one agent, one role, under this work. Provider is the existing cockpit `.modes` seg, model is a free-text input (CLI model names churn — no hardcoded list, empty = CLI default), `real` is the same dry/real checkbox as the request bar. **Place (v6.0 P4) is a third `.modes` seg — `worktree`(default, today's muscle memory) │ `in-place`** — with one mono `.sheet-tradeoff` line under it stating the cost of each (`in-place = 체크아웃 공유, 순차 · worktree = 병렬, 나중에 머지`): the tool never implies you get both. The footer hint (`#agentPlaceHint`) swaps with the choice, so placement is never implicit. `.sheet-err` is the **inline** failure line (`--failed`, no new color): the `IN_PLACE_BUSY` 409 keeps the sheet open and prints *steer it, or launch in a worktree* where the choice was made — a toast that vanishes would hide the one thing the human must act on |
| In-place marker | the run row's existing `.meta` reads `r12 · in-place` (tree) + `in-place` on the Review compare merge button (disabled) | an in-place run has **no branch chip, because it has no branch** — its edits land in the checkout. Nothing new is drawn: the id line gains three words and the merge affordance states why it is off (`체크아웃에서 직접 작업 — 머지할 것이 없습니다`), matching the server, which refuses merge/PR/sync on the same root-session marker. The compare diff below it is the checkout's uncommitted changes, said plainly in the button title |
| Tab state dot (v5.28 A4) | `.as-dot` + `data-role="asdot"` on every terminal tab (`kind!=='viewer'`), after the name | the tab's **second** axis: the leading `.st` dot is the run's status, this one is what the agent's terminal is doing right now — `working` `var(--running)` · `waiting` `var(--blocked)` (pulses, `@keyframes aspulse`, off under `prefers-reduced-motion`) · `idle` `var(--faint)` · `exited` `var(--done)`. **All existing tokens — zero new colors.** A run with no live agent state gets no class and the dot is `display:none`, so untracked tabs look exactly as before. Painted from the server's `agentstate` stream only (`paintAgentState`) — the client never guesses a state or synthesizes a transition |
| Tree-dot agentstate override | the same four `.as-*` classes appended to a tree run/session row's existing `.st` dot | **the tree is the rail** (v5.28 A4 revised): a separate Agents rail would duplicate the v6.0 Project ▸ Work ▸ Session tree, so agent state paints the run rows the tree already draws. The `.as-*` rules sit *after* the `.st.*` status rules (same specificity → later wins) and clear the running ring. Rows without agent state keep today's run-status coloring untouched, and **tree order never changes** — no attention re-sorting: the tree navigates, the chip calls |
| Waiting chip | `#waitChip` (`◔ N`, `#waitN`) in the cockpit header | the ambient glance: how many agents are waiting on you, shown **only when N>0**. Clicking focuses the next waiting terminal, cycling through them (already-open tab → focus its pane; otherwise opened as a tab). Lives in `<header>`, so it survives focus mode (`.layout.focusmode` hides only `.rail`/`.reqbar`) and the ≤860px header — attention must outlive the chrome it rode in on. Amber like every other blocked affordance (`--blocked` on the `.wip`/`.chip.blocked` wash), never a fresh color |
| Attention popover (v5.28 A5) | `#attnBtn` (mono `◎` header `.toggle` ghost, `aria-haspopup`/`aria-expanded`) → `.apwrap` > `.apop` anchored panel | the cockpit has **no settings surface** and does not grow one for three preferences — they live in a small popover off one header button, next to `#waitChip`. Three rows, all **existing components**: `소리` and `브라우저 알림` are `.rchk` checkboxes, `울릴 전이` (대기만 │ 완료만 │ 둘 다) is the `.modes`/`.mode` seg, plus an `.apop-note` line stating the contract ("전부 기본 꺼짐. 보고 있는 탭은 자신을 울리지 않습니다"). Persisted in `localStorage` as `coxpit.sound` · `coxpit.notify` · `coxpit.pingOn`, **all off/both by default**. Enabling notify runs `Notification.requestPermission()` and falls back to off + an error toast on denial (the browser's permission is the SSOT — a remembered "on" without permission reads as off). `.toggle.on` tints the header button brand while anything is on. Esc or focus leaving the wrapper closes it; focus is visible via the global `:focus-visible` ring. Panel is `--surface` on `--line-hi` — **zero new colors** |
| Attention delivery | `raiseAttention(runId, prev, next)` inside `paintAgentState` | the audible half of the v5.28 A4 dots. Only a transition **into** `waiting`/`exited`, only when `document.hidden` or the run isn't the focused tab, only if the `coxpit.pingOn` filter admits it — the focused tab never pings itself, and the dots/`◔` chip stay always-on regardless of prefs. **Sound** is a two-tone WebAudio blip generated in code (no audio asset, no new file), quiet on purpose. **Notification** body is `<세션 이름> · 입력 대기` / `· 완료` — the run's `runLabel()` and the state, **never terminal content**; clicking it focuses that tab through the same `openTab` jump the chip uses. Server twin: `src/agentstate.ts` POSTs `{event:'agentstate', runId, state, url?}` to `COXPIT_WEBHOOK_URL` on the same transitions, **state only** (the endpoint is outside the trust boundary) with a 60s per-run cooldown |
| Icon system | inline Lucide `<symbol>` sprite (`src/icons.ts` → `ICON_SPRITE`) + `.ic` class (`ICON_CSS`) | **real Lucide SVG paths** (ISC, `licenses/lucide.txt`) copied verbatim — **no CDN, no runtime dep** (the board is one HTML string under a strict CSP). A hidden `<svg style="position:absolute;width:0;height:0">` of `<symbol id="i-…" viewBox="0 0 24 24">` sits at the top of `<body>` in **both** `board.ts` and `login.ts`; usage is `<svg class="ic"><use href="#i-x"/></svg>` (static) or the client `ic('x')` helper (`ICON_JS_HELPER`, for dynamically-built HTML — sets `.innerHTML`, never `.textContent`). `.ic{width:1em;height:1em;stroke:currentColor;fill:none;…}` → **size follows font-size, color follows `currentColor`** so status classes (`.req.warn`, `.card.selected .selbox`, toast tints) recolor the icon automatically. Swapped: lock/unlock (login), terminal · folder · branch (`⎇`) · merge · play (Run) · check (done/select) · x (close) · refresh · recycle (Reclaim `♻`) · pencil (steer) · message (answer `◆`/review) · file (doc) · image (page/file) · code · chevron · chevron-down (model combo caret) · plus (`＋`, also count `+`) · minus (count `−`) · external-link (remote `🔗`/PR) · alert-triangle (missing deliverable) · bell/bell-off (notify). Left as text: prose arrows (`→`), dropdown carets (`▾`/`▸`), goal-band marks (`⌒`/`⌁`/`↳`), CSS status dots. Extend by adding one `<symbol>` (a real Lucide path) — never a font, never a CDN |

## Interaction rules

- **No native UI.** System dialogs/selects break the instrument feel and can't be themed. (Desktop-shell OS menus are the one exception — they should feel native to the OS.)
- Destructive = `confirmUI` with `danger: true` and an explicit consequence sentence. Never double-confirm.
- Feedback for every mutation: success → `ok` toast, failure → `error` toast carrying the server's `detail`.
- Focus is always visible (`:focus-visible` teal ring). Esc closes the topmost layer.
- Motion is functional only (pulse = running, flash = updated) and disabled under `prefers-reduced-motion`.
- Copy: short, lowercase-leaning, concrete ("worktree missing on machine", not "an error occurred").

## Extending

New UI goes through this file first: reuse a component; if none fits, add the component *here* (tokens + rules) in the same commit that introduces it. If the board ever splits into multiple pages or the Next.js front lands, these tokens move to a shared stylesheet unchanged.
