# Coxpit Design System

The board is a single self-contained page (`src/board.ts`), so the design system lives as CSS custom properties plus the component classes below. **Every new piece of UI must be built from these — no native browser UI** (no `alert`/`confirm`/`prompt`, no bare `<select>`, no default buttons).

## Identity

A dark fleet console: an instrument you *operate*, not a document you read. Data is monospaced, chrome is quiet, one teal accent means "coxpit is acting". Semantic status colors are separate from the accent and never used decoratively.

**Positioning (v6.0).** There are two surfaces and they are not peers: the **cockpit** (`/cockpit`) is the home — terminals organized as Project ▸ Work ▸ Session — and the **board** (`/`) is the reading room, where you review and look things up. New affordances land in the cockpit; the board is kept working, not grown. Nothing was removed from it, so a fleet-first user keeps every route, view and habit they had.

## Tokens (`:root` in board.ts)

| Token | Value | Use |
|---|---|---|
| `--bg` | `#0b0d12` | page ground |
| `--surface` / `--surface2` | `#12151c` / `#171b24` | cards, panels / nested panels, chips |
| `--line` / `--line-hi` | `#222835` / `#2f3648` | borders / hover borders |
| `--ink` / `--muted` / `--faint` | `#dee4ec` / `#8792a2` / `#5c6675` | text hierarchy (3 steps, no more) |
| `--brand` / `--brand-ink` / `--brand-dim` | `#4ec9b0` / `#062822` / 13% teal | the one accent; ink-on-brand; washes |
| `--s-*` | pending gray · preparing amber · running blue · done green · failed/error red · stopped violet · merged teal | status only — never decoration |
| `--mono` / `--sans` | ui-monospace stack / system stack | data vs UI text |
| `--r-card` / `--r-ctl` | 10px / 8px | cards / controls |
| `--shadow` | `0 8px 28px rgba(0,0,0,.35)` | floating layers only |

Type: UI text 13–14px sans; data (ids, branches, logs, diffs, versions) always mono; numbers in tables get `tabular-nums`; section labels are 10–11px mono uppercase with wide tracking.

## Components

| Component | Class | Rules |
|---|---|---|
| Buttons | `.btn` `.btn-ghost` `.btn-danger` (+`.sm`) | one solid teal primary per view; ghost for secondary; danger is outline red, reserved for destructive |
| Status chip | `.chip` (+`.running`) | dot + uppercase mono label, colored by `--s-*`; running pulses (respects reduced-motion) |
| Cards | `.card` | hover = border lighten + 1px lift; whole card is the click target |
| Inputs | `input, textarea` | `#0e1118` well, teal focus border; placeholders in `--faint` |
| Dropdown | `.dd` (wraps a hidden `<select>`) | `dressSelect(id)` enhances; native select stays in DOM as the state holder so `$(id).value` contracts hold; ✓ marks current; Esc/outside closes. The sheet selects (`#repoMachine`/`#taskRepo`/`#taskCapture`) are all dressed so their **popup is a dark `.dd-panel`, never the OS list**. Any still-bare `<select>` (e.g. `#archRepo`) also gets `appearance:none` + a token chevron background so no white native arrow/popup ever leaks |
| Segmented | `.seg` / `.seg-opt` | binary/ternary mode choices; **slim single row** (`.seg-opt` is `inline-flex` row — never a `flex-direction:column` stack, which reads boxy). The risky option gets brand fill; its mono `spends credits` hint (`.seg-hint`) rides **inline and only appears once the option is selected** so the resting seg stays clean and matches the type/provider seg height |
| Count stepper | `.stepper` (`#taskCountStep`) around `input[type=number]` | native number spinners render white — killed with `input[type=number]{appearance:none}` + hidden `::-webkit-*-spin-button`; replaced by token `−`/`+` `.stp` buttons (`#cntDown`/`#cntUp`, ghost feel, `#i-minus`/`#i-plus`) flanking a centered `tabular-nums` field, clamped 1..8 (`stepCount`, manual typing re-clamps on `change`). The `<input>` stays the state holder (`$('taskCount').value`) |
| Toast | `toast(msg, 'ok'\|'error')` | top-right stack, auto-dismiss ~4s; for outcomes and errors — never questions |
| Confirm | `confirmUI(msg, {sub, danger, okLabel})` | promise-based dialog; every destructive action goes through it with a consequence line in `sub` |
| Modal | `.overlay` + `.modal` (+`.wide`, `.term`) | backdrop blur, Esc/backdrop closes; header = id · title · chip · × |
| Empty state | onboarding `.setup` panel | never a bare "no data" — show machine readiness + next steps |
| Field label | `.flabel` | 10px mono uppercase over every form control — placeholder-only fields are not allowed |
| Repo browser | `.brw` modal + `GET /api/browse` | pick-don't-type: navigate folders, git repos get a badge + inline Register; local machine only (remote = manual path) |
| Export dialog | `#expOverlay` (`.cfm` variant) | one input + consequence line; empty dest falls back to `~/coxpit-exports/r<id>` |
| PR affordance | `Open PR` ghost in compare · `PR` + `#i-external-link` link on cards | once a run has a PR, the button becomes the link — never both |
| Select mode | `Select runs` toolbar toggle + `.selbar` | cards grow a ✓ circle; click toggles instead of opening; the floating bar states the consequence ("merges in selection order · conflicts spawn an integration agent") |
| Plan form | `#planForm` sidebar section | one goal → planner splits → auto-launch; button narrates its long wait ("Planning… (1–3 min)"); follows the global Dry/Real mode |
| AI review panel | `#cmpReview` above compare columns | reviewer digests every diff into approach/pros/cons/recommendation (mdLite-rendered) — the human judges, not reads | — full-height scrollable pane; the AI-review button toggles it in place of the diff columns
| Timeline lines | `humanize()` (**shared** — `src/humanize.ts`, embedded in both `board.ts` and `cockpit.ts`) | never raw JSON: `said`/`tool ▸ Name — file`/`done`/`steer`/`ask ?`/`sync`; rate-limit and tool-result echoes are dropped as noise. The modal always refetches the full timeline on open (`GET /api/runs/:id`) — the fleet payload caps events at 40/run |
| Session bar | Work/Ask mini-seg + input in run modal | the run IS a session: Work = next instruction, Ask = question only (no file changes); Sync base refreshes a long-lived worktree |
| Notify bell | `#bell` header toggle (`#i-bell-off`/`#i-bell`) | browser notification on run settle; server-side twin is `COXPIT_WEBHOOK_URL` (JSON `run.settled` POST) |
| Navigator rail (v5.0) | `aside.rail` = machine switcher → Repositories(`#repoList`) → View(`#viewNav`) → spacer → `#newBtn` → Library | the rail **navigates**, it does not compose: a compact machine button, a scannable repo list with live counts + scoping, the moved view nav, and a single `＋ New` that opens the launcher. Never put a permanent compose form back in the rail — launching is a focused action behind `＋ New`. (Supersedes the v4.x Context→Start→Library layering) |
| Machine switcher | `.machine` button (`#machineSwitch`, `#i-server` + `.mdot2` reachability) + `.mmenu` menu | replaces the machine `<select>`; shows the current machine + an online dot; click opens a small anchored menu of machines; the hidden `#repoMachine` `<select>` stays as the state holder so every `machineSlug` contract still reads `$('repoMachine').value` |
| Repo row | `#repoList .repo` (`#i-folder` · name · `.cnt` active-run badge · `.attn` dot) | one row per registered repo; **counts are computed client-side** by grouping the loaded `/api/fleet` active runs by `tasks.get(run.taskId).repoId` (no endpoint); a failed/error run raises the red `.attn` dot; the selected row is `.repo.on` (persisted `localStorage('coxpit.repo')`). Row hover reveals a `.rowmenu` with per-repo **base-branch ⎇** and **remove ×** (`.rmbtn`, reusing `openBranchFor`/`removeRepoById`) styled as **icon-ghost** (transparent bg · `--muted` `#i-branch`/`#i-x` icon · hover → `--ink` + `--surface` fill + `--line-hi` border) — never a white button. The in-sheet repo-management row (`#repoActions`: Browse/New/Path/⎇/×) uses the same `.btn-ghost` dark treatment. An **All repositories** pseudo-row (`data-repo="all"`, `#i-layers`) clears the scope. Empty state = an "Add your first repository" CTA. Scoped under `#repoList` so the Library capture `.repo` rows are untouched |
| View nav | `#viewNav` `.navi` (Active `#i-layers` / Goals `#i-target` / Archive `#i-archive`), each with a mono count | moved out of the header (the old `#viewSeg` is gone): **Active** = active runs flat (repo-scoped), **Goals** = the group bands only (client-side filter to grouped tasks, rendered as the existing `.gband`s), **Archive** = the `/api/archive` list (repo-scoped via `?repo=`). Reuses the existing `setView`/`render` logic — only the entry point moved. `.navi.on` = `--surface2` fill + brand icon |
| New sheet (v5.0 Part B) | `#newBtn` → `.overlay#newSheet` → `.sheet` card: header (`＋ New · <selected repo>` from `#sheetRepoLbl` + × `#sheetClose`) → **type seg** `#launchTabs` (Task│Goal│Workbench) → `#taskForm` (`.sheet-body` scroll region + `.sheet-f` footer). The launch **logic is relocated, not rewritten**: the same `#taskForm` submit handler branches on `lTab`, and the hidden `#taskRepo`/`#repoMachine` `<select>`s still hold state | a focused compose sheet, not a permanent rail form. `setV(tab)` swaps the body panel **and adapts the footer**: **Task** = provider seg + progressive **Options** reveal → Dry/Real · count `#taskCount` · **Run fleet**; **Goal** = `#planGoal` + provider seg → Dry/Real · **Plan & run** (no count — the planner decides N); **Workbench** = optional name + explainer → **Open workbench** only (mode seg + count hidden). Target repo = the rail-scoped repo; `＋ New` opens on **Task**. Closing resets the repo picker affordances |
| Options reveal | `.opt` disclosure in the Task body (`#taskOptToggle` `aria-expanded` + `#taskOptBody`, `.opt-car` chevron rotates) | the rarely-used Task fields — **model · design capture · deliverables** — collapse under a single "Options" row (subtitle `model · design · deliverables`); collapsed by default, expanded state remembered per session (`sessionStorage('coxpit.taskOpt')`). Keeps the common path (title · prompt · provider · run) to one glance |
| Repo picker (in-sheet) | `#repoActions` row (Browse/New/Path/⎇/×) + `#repoForm`, both `hidden` by default | reached from the rail **Add repository…** (`#repoAdd`) which opens the sheet and unhides the row → the existing Browse modal (New-folder/Path inside). Per-repo ⎇/× also live on the rail repo-row hover menu (Phase 1); these are the sheet-side fallback wired to the same `openBranchFor`/`removeRepoById` |
| Workbench | launcher tab + `--s-open` status | worktree+tmux, no agent: humans (and their interactive claude) work inside; the card keeps the diff/merge/PR/export rails |
| Terminal | full-screen view, xterm themed with tokens | not a modal — `#termOverlay` fills the viewport (padding 0, no radius); background `--bg`, cursor `--brand`; CJK-safe font stack + unicode11 widths; Esc or × closes. **Links open, never ask**: plain URLs go through `WebLinksAddon` and OSC 8 hyperlinks through a `linkHandler` in the Terminal options — both `window.open(uri,'_blank')`, which the desktop app turns into the system browser. Without that `linkHandler` xterm falls back to its own default, a **native `confirm()`** (forbidden here) followed by an empty `window.open` the desktop app denies — an OSC 8 link that did nothing at all |
| Terminal tabs | `.term-tabs` / `.ttab` in the terminal header | every live session (`running`·`open`) is a tab — switch sessions without leaving the terminal; active tab = `--brand` border; re-rendered on every board update |
| Provider seg | `#provSeg` in the Task panel | Claude/Codex per launch (persisted in localStorage); Task tab only — the Goal planner and Workbench are provider-neutral; cards already carry the agent name as the badge |
| Pocket board (v5.0 Part C) | `.menu-btn` (`#menuBtn`) → `aside.open` off-canvas drawer + `.scrim` + FAB (`#fab`), all ≤860px | the fleet is the first screen. The drawer is **just the navigator** (machine switcher · repo list · view nav) — the compose form is no longer crammed into it. A floating **`＋` FAB** (`.fab`, mono brand disc, Lucide `#i-plus`) opens the same `#newSheet` compose sheet (full-bleed on mobile via the existing mobile-modal CSS) and closes the drawer; the rail's inline `#newBtn`/`.newnote` are hidden on mobile (`#newBtn,.newnote{display:none}`) so there's one launch affordance. FAB is desktop-hidden (`.fab{display:none}` base → `inline-flex` only in the ≤860px query). Reuses the v3.6 drawer plumbing (`setDrawer`, `translateX(-103%)` off-canvas + `aside.open{translateX(0)}`) |
| Mobile stability | root guard in the base `:root`/`html`/`body` block | kills the iOS left-right rubber-band + horizontal pan so the PWA feels native: **`html{overscroll-behavior:none}`** (stops the rubber-band) + **`body{overflow-x:hidden;overscroll-behavior-x:none}`** (clamps horizontal overflow). **Never `overflow` on `<html>`** — that breaks iOS `position:fixed`/sticky, which the fixed header and the off-canvas drawer both depend on; clamp **body only**. The off-canvas drawer (`aside` `translateX` off-screen, `overflow-x:hidden`, `max-width:100vw`) is clipped by the body clamp so it never joins the scrollable width. Wide content (diffs/code/tables) still scrolls inside its own `overflow-x:auto` container; `viewport-fit=cover` + `env(safe-area-inset-*)` respected (FAB/term-ibar offsets) |
| Mobile modals | full-bleed at ≤860px | run modal stacks Timeline over Diff (1fr/1fr rows); compare columns stack vertically; footers wrap; inputs go 16px (iOS focus-zoom guard) |
| Deep link | `/?run=N` + notification `onclick` | any surface (webhook message, browser notification) can land directly on a run modal; the param is consumed then stripped from the URL |
| Doc mode | `#cmpDocsTgl` → `docsHTML()` (compare only) | Rendered ↔ Diff toggle in the compare panel; changed `.md` renders via mdLite, `.html` in a fully sandboxed iframe (no scripts). The run modal's own toggle is **subsumed by the v4.7 output cards** — diff is the `code` card, rendered docs/pages are `doc`/`page` cards |
| Contract strip | `.contract` / `.req` (run modal, under the header) | `Required outputs (contract): [icon type]… — Extra: …` from `GET /api/runs/:id/outputs`; `#i-check` present (green), `#i-alert-triangle` declared-but-missing (amber `.req.warn`), Extra (incidental) is dashed `.req.aux`; hidden when a run declares nothing and produced no cards |
| Output cards | `#outCards` + `.ocard` (run modal right column) | one card per deliverable — a typed Lucide icon (`OUT_ICON`: answer→`#i-message` · code→`#i-code` · doc→`#i-file` · page/file→`#i-image`) + title + `Required`/`Extra` badge + meta + `#i-chevron`; missing declared cards render dashed (`.miss`, "output not met") and are not clickable; `pickDefaultCard()` auto-opens the run's primary deliverable (doc/answer when no code, else the declared doc, else code, else first present) |
| Output viewer | `#outDetail` + `.oback` (swaps in over the cards) | per-type real render: `answer`/`doc` → mdLite (`.doc-md`); `page` → live `sandbox=""` `srcdoc` iframe (`.doc-frame`); `code` → the colored `<pre class=diff>` renderer with `.dl-line` steer clicks; `file` image → `<img class=oimg src=/api/runs/:id/file>` (escA'd path), other file → `.odl` download link (+ small text → mono `<pre>`); `‹ 산출물 목록` returns to the list |
| Diff comments | `.dl-line` (run modal `code` card viewer) | every +/- line is a click target that quotes `file: "line" — ` into the steer input; hover = brand tint; hidden runs (not settled) ignore clicks |
| Deliverables selector | `#taskOutputs` + `.ochip` (Task compose, inside the Options reveal) | optional contract — chips `Answer·Code·Doc·Page·File` toggle the declared `tasks.outputs`; default none = today's behavior (no contract, run shows whatever it produced); selecting any sets the contract + prompt injection; on = `.brand-dim` fill |
| GitHub import | `#ghOverlay` (`.cfm` variant) + `#ghImport` in Task panel | fetch drafts the form — never auto-launches; the human reviews, picks a provider, runs |
| Share affordance | `Share` ghost in run modal footer | mints/reuses one read-only link per run, copies to clipboard; the share page is server-rendered, script-free, action-free |
| Swarm lineage | `↳ by rN` card meta + `swarm` timeline line | subtask cards carry their parent; the parent's timeline logs each spawn (`↳ spawned task #N`) |
| Terminal input bar | `.term-ibar` + `.tkey` (touch/≤860px) | direct xterm typing breaks IME composition on software keyboards (jamo arrive decomposed) — the bar is a real input that composes natively and sends whole lines; esc·tab·^C·↑·↓ chord keys; safe-area padded |
| Viewer toolbar (v6.4) | `.modal#histModal` > `.pick.hist-pick` header: `.hist-modes` (대화 │ 터미널) + `.hv-tools` of `.hv-btn` — `↵` `#histWrap` · `⌕` `#histFind` · `↕` `#histJump` · `↻` `#histRefresh` · `×`; the input-bar entry `#histBtn` is promoted with `.tkey.hv-main` | xterm is for typing and cannot be made touch-friendly, so the read-only viewer is where a phone **reads and grabs**. Mono glyphs only (no emoji); `.hv-btn.on` = `--brand` text + border (wrap on, find open). The header wraps (`flex-wrap`) and the title ellipsizes so the tools never overflow a narrow screen |
| Viewer lines (v6.4) | `#histLines.hlines` of `.hln` rows = `.hln-tx` (linkified line) + `.hln-cp` `⧉` | the terminal tab no longer pours scrollback into one `<pre>`: one DOM row per logical line (`renderHistLines`). `.hln-url` (`--running`, underlined) — a **real anchor** (`href` + `target="_blank"` + `rel="noopener noreferrer"`, `data-url` kept for the copy paths): a plain tap **opens** it, and long-press / open-in-new-tab / ⌘-click work because the browser owns them. It used to copy instead, which made the one underlined thing on the page the one thing that would not open — and on a phone (the viewer is the main reading surface) there was no way to open a URL at all. Copy is not lost: the row's `⧉` and the row background still copy the raw line, URL text included. `histClick` only `preventDefault()`s the anchor when a text selection is live (selection beats navigation). The desktop app's `setWindowOpenHandler` routes `target="_blank"` to the system browser; `.hln-path` (`--brand`, underlined) — tap opens it through the same `openPathFromTerm` the terminal link provider uses; tapping `⧉` or the row background copies the raw line. `.hln-tx` is `user-select:text`, and a tap is only a tap under 250ms / 10px, so long-press native selection passes through. `.hlines.nowrap` (from `↵`, persisted as `coxpit.histwrap`) turns lines into single-row horizontal scrollers for long commands. All copying goes through the one shared `copyText(s,label)` the terminal select-to-copy uses |
| Viewer code block (v6.4) | `.hln-code` (`--panel` block, `overflow-x:auto`) + `.cb-cp` `⧉ 복사` inside chat `.ch-bubble` | fenced code in an answer renders as a mono block with its own copy button; prose around it is linkified. Bubbles are selectable and read like a page (line-height 1.7, `keep-all`, assistant bubbles full width). `↵` off affects code blocks only — prose always wraps |
| Viewer find (v6.4) | `.hist-findbar#histFindBar` (input · `n/m` `.fc` · `‹` `›` · `×`) under the viewer header; hits are `mark.hl`, the current one `mark.hl.cur` (`--blocked`) | case-insensitive, client-only over the fetched text of the active tab (button glyphs excluded). Starts at the newest hit; Enter = next, ⇧Enter/`‹` = previous; Esc closes the bar first, then the viewer |
| Viewer latest FAB (v6.4) | `.hist-fab#histFab` (`↓`, round, `--brand` on `--brand-ink`, bottom-right of the viewer) | appears once the active tab is scrolled more than 40px above the bottom; tap = back to the latest. `↕` in the toolbar toggles top ↔ bottom |
| Viewer alt-screen note (v6.4) | `.hist-note#histAltNote` — one faint `--faint` line on `--panel` above the lines, shown only on the 터미널 tab | a full-screen TUI (claude, vim, less) lives on tmux's alternate screen, which has no scrollback, so the terminal tab can only ever show the current screen. Rather than look broken, the screen says so and names the way out ("전체 대화는 '대화' 탭"). The viewer opens on 대화 for exactly that reason; no icon, no color — the note must not compete with the content |
| Open tabs restore (v6.4) | no new component — the existing cockpit tab bar, backed by the **daemon** (`GET`/`PUT /api/ui/opentabs` → `{ids, active}` in `<dataDir>/ui-state.json`), with `localStorage 'coxpit.opentabs'` kept only as an offline copy, beside the layout snapshot `'coxpit.session'` | **a reload must not close work that is still running.** The layout snapshot only serializes the tabs that are *placed in a pane*, and a phone has exactly one pane — so every other open tab vanished on reload or a daemon restart while its tmux session was still alive. The tab set is remembered separately (run tabs only, most recent 24) and reopened on the first `hydrate()`, **skipping any run the fleet no longer has** — a dead run is never resurrected as a tab, and a tab the owner closed is never reopened (`closeTab` persists the reduced set). It is remembered **on the daemon, not in the browser**: a browser store dies with a cache clear, a new device or an old cached page, which is exactly why the first attempt at this never reached the phone. The client reads the server on hydrate and falls back to the local copy only when that read fails or when the daemon has never stored a set (`stored:false` — a one-time migration; an *empty* stored set means the owner closed everything and is obeyed); writes are debounced 400ms so rapid focus changes are one request. The previously focused tab is restored only when there is a single pane; a desktop split layout keeps what the snapshot rebuilt. Twin rule, same reason: **the three served pages (`/`, `/cockpit`, `/hud`) answer `Cache-Control: no-store`** — each one carries its own JS inline, so a cached document is a frozen app and no fix can land. Versioned `/vendor/*` and `/brand/*` stay cacheable. Nothing drawn, no schema |
| Doc provenance | `.doc-src` line above the rendered docs | when the docs come from a snapshot (worktree gone after merge/close), a faint mono line says so — the reader knows they're seeing preserved output, not a live read |
| Model input | `.mcombo` (`#modelCombo`) = `#taskModel` free-text input + a `.dd`-style recent-models menu (`#modelMenu`, caret `#modelTog`) | free text stays (CLI model names churn — no hardcoded list), empty = CLI default. The old native `<datalist>` popup rendered white and un-styleable → **replaced by a dark `.dd-panel` menu** of the last-5 (localStorage); picking one fills the input, typing is unchanged. Esc/outside closes; the card meta shows `⚙ model` when set |
| Base branch dialog | `#brOverlay` (`.cfm` variant), reached from the repo-row hover `⎇` (v5.0) or the New-launcher `#repoBranch` | merge · Sync base · PR all target it; validated against the repo (must exist); `openBranchFor(id)` captures the target repo id so it works from either entry point |
| New project | `#npOverlay` (`.cfm` variant) + `New` in the New launcher's repo-management row | the only place coxpit ever runs `git init`; empty/missing/commitless paths only — a populated folder is never initialized; success auto-selects the repo |
| Remote access | onboarding section (`#rmtOnboard`) + `#remoteOverlay` (`.cfm` variant) via header `#i-external-link` | detect Tailscale; one-click **Serve** (tailnet, safe, default) shows/copies the HTTPS `.ts.net` URL; **Funnel** (public) is the risky red toggle, disabled unless auth is set; non-Tailscale users get copy-paste recipes (`.rmt-more` details) with the real `config.port`. coxpit drives the tool, never hosts a relay |
| Commitless register | `NO_COMMITS` 400 → confirm offers greenfield | register never silently stores a broken defaultBranch; the confirm (Register form + Browse Register) reuses `#npOverlay`'s POST to seed an empty initial commit |
| Close guard | danger `confirmUI` on 409 from close | closing deletes worktrees — if any run has unmerged, unexported, non-PR changes the server 409s with `atRisk`; the dialog lists them (`rN · M files`) and only `force:true` proceeds |
| Closed card | `.card.closed` (dim + hatched `.log` + CLOSED stamp) | a closed card must read as closed at a glance — desaturated, diagonal hatching over the timeline, a rotated CLOSED stamp; the status chip stays (done/merged history) |
| Group band | `.gband` (`grid-column:1/-1`) around sibling cards | goal (`⌁`) / swarm (`↳`) siblings cluster under a header (title · N tasks · M settled) with fold/Select runs/Close group — a **dashed border**, never a left accent bar or a color fill; cards inside stay first-class, single-run tasks never band |
| Attempt counter | `.attempt` in card meta | any task with >1 run shows `run i/n` so same-title cards read as attempts, not duplicates; brand-tinted, mono |
| Group actions | band header buttons (delegated on `#grid`) | Select runs reuses the existing select mode + selbar (no new merge path); Close group loops the same close endpoint honoring the v4.1 guard, aggregating every `atRisk` into one confirm; fold state persists in localStorage across WS repaints |
| View seg (retired) | ~~`#viewSeg` in header~~ → moved to the rail `#viewNav` (v5.0) | the Active/Archive header seg was **removed** in v5.0 and re-homed in the rail as `#viewNav` (with a new **Goals** entry); the closed-count badge now rides the Archive nav row (`#navArchiveN`). Archive is still a static snapshot (WS updates ignored while shown) |
| Reclaim worktrees | `#reclaimBtn` (`#i-recycle` Reclaim · N · ~XMB, and `(+M flagged)` when some are risky) in the Archive `.arch-bar` | a maintenance affordance that reclaims disk debt — run worktrees left behind by finished runs (~180MB each, node_modules lives inside). Fed by `GET /api/worktrees` (`{items,totalKb}`, each item carrying `reclaimRisk`); **hidden when nothing SAFE is reclaimable**, and the count/size it shows are the **safe** subset, because the bare prune-all it posts reclaims exactly that subset. Click → danger `confirmUI` ("remove N finished run worktrees? … worktrees holding unmerged, un-exported changes are left alone — tick those one by one in the cockpit") → `POST /api/worktrees/prune` → toast the reclaimed count + refresh. Safety is server-side: the endpoint lists **closed** tasks, **failed/error/stopped** runs and (v6.0 T6b) settled **done/merged** runs that own an isolated worktree — running/preparing/pending are never eligible, and a bare prune-all skips every `reclaimRisk` item |
| Archive rows | `.arch-row` in `#archive` | closed tasks as compact single-line rows (title · run summary · repo · date), newest first, with a debounced title search and repo filter + load-more paging; a row opens the run modal via `GET /api/tasks/:id` — diff says worktree gone, Rendered falls back to the v4.1 snapshot |
| Converge cockpit | `.runs` + `.run` decision rows (workroom Work body) | the Goal workroom's Work mode is a **per-run decision panel**, not a merged timeline: one `.run` row per group run — collapsed head = checkbox · status dot · `rN` · title · meta · inline `[Review][Steer][Merge][Close]` · caret; merged rows dim + `merged → base` badge (action → `Open`), running rows lock (`Open · decide once settled`). Expanding a row loads the run's **P2 output cards** (`roomLoadRunOutputs` → `outCardHTML` + `renderOutCardInto`, no duplicate renderer) as the review peek + an inline steer input (`Steer → continue session`); `[Review]` runs `reviewTask` and shows the `#i-message` summary inline. All actions reuse existing endpoints (`/runs/:id/merge·steer`, `/tasks/:id/review·close`) — no new decision logic; Ask mode is unchanged |
| Group action bar | `.gbar` (workroom, above the runs) | `☑ N selected · Integrate selected (N)` (brand, reuses select-mode + `/api/integrate`) · spacer · `Review all` (`reviewTask` loop) · `Close group` (danger, reuses `closeGroup` guard); Work-mode only (hidden in Ask) |
| Terminal guard | `#mTerm` disabled + tooltip (`termUnavailReason`) | a run whose task is closed or whose worktree was cleaned (merged/cleanup → empty `worktreePath`) has no live tmux session — the Terminal button is disabled with a "worktree cleaned — terminal unavailable" tooltip. Server twin: `cleanupRun` blanks `worktreePath`/`tmuxWindow` so `getRunTermInfo` returns null and `/ws/term/:id` closes with a clear reason instead of attaching a dead session |
| Login / setup page | standalone HTML (`src/login.ts`), served by `authGate` on an unauthenticated **HTML GET** (200, never a Basic popup) | the branded replacement for the browser's native auth dialog — a single centered `.card` on `--bg`, the mono **coxpit** mark + a lock/unlock glyph, **no username** (key-only, single-owner). **Unlock**: one `access key` password field + a "Remember this device" checkbox (30-day cookie vs session) + a mono `.err` rate-limit line ("wrong key — N attempt(s) left" / "try again in Ns"). **Setup** (first-run, no key configured): adds a `confirm access key` field and a `setup token` field with an explanatory `.hint` (paste the one-time token from the daemon log unless you're on `http://127.0.0.1` directly — anti-claim). A footer link points at the remote-access docs. **Submit = a real form navigation POST** (v5.0 Part C — `<form method="post" action="/api/auth/{unlock,setup}">` with a hidden `nav=1` flag, urlencoded): the server answers **303 → `/`** on success so the **browser commits the `Set-Cookie` on the navigation response** before landing on the board — this fixes mobile Safari, which dropped the fetch-response cookie before the old `location.replace('/')` and bounced back to login. Failure re-renders this same page server-side with the error inline (`loginPageHTML(setup,{error})`), so it works with JS off; client JS only pre-validates (empty key, confirm-match, min-length) and disables the button. The **JSON API is unchanged** (`content-type: application/json`, no `nav`) → still `{ok}`/201/401 for automation. Key inputs carry `autocapitalize="none" autocorrect="off" spellcheck="false"` and go 16px (iOS focus-zoom guard); the mono `.err` line surfaces the rate-limit block ("too many attempts — try again in Ns"). Auth is **exposure-conditional**: a loopback-only bind (`127.0.0.1`) is trusted-local and never shows this page; only an exposed bind (`0.0.0.0`/routable) or `COXPIT_AUTH_PASS` on an exposed host gates. Uses board tokens verbatim; the lock/unlock marks are the Part-B Lucide icons (`#i-lock`/`#i-unlock`) |
| Work tree (v6.0 T1) | cockpit `#tree` — `.lbl` **Scratch** section first (see Scratch section, v6.0 S1), then one section per repo: `.tnode.repo` → `.tnode.goal` band → `.tnode.task` (the work) → `.tnode.run` (the session) | the tree *is* the hierarchy Project ▸ Work ▸ Session; names live on the work, roles on the run. A run row reads `role · model` (e.g. `구현 · opus`) with the status dot on the left and a faint mono `rN` in `.meta` — the id never disappears, it just stops being the name. A group/goal renders as a **thin dashed band** inside its repo section (same rule as the board's `.gband`: dashed border, never a left accent bar). Machine scoping unchanged; the same render path fills the ≤860px drawer |
| Tree node action | `.tact` on a `.tnode` (`data-newwork` on the repo row · `data-workmd`/`data-newagent` on the work row) | the affordances that *act* from the tree: `＋ 새 작업` (repo), `▤ WORK.md` and `＋ 에이전트` (work). Icon-ghost mono text — transparent, `--faint`, revealed on row hover (`.7` opacity on touch, where hover does not exist), hover → `--brand` on a `--surface2` fill. Never a filled button: the tree navigates, and one row must not out-shout the rest. Two actions on one row: only the **first** takes the `margin-left:auto` (`.tact+.tact` resets it) — two auto margins would split the free space and drift apart. **On the repo row the reveal is `display`, not `opacity` (v5.28 D-rail)** — `opacity:0` hides pixels but keeps the width, so at rest the invisible buttons were eating the project name's space |
| Repo-row overflow (v5.28 D-rail) | `.tact.tmore` (mono `⋯`, `data-more`, `aria-haspopup`/`aria-expanded`) on `.tnode.repo` → the shared `.rmenu#rowMenu` panel | **the rail's width belongs to the name.** Three `.tact`s on one `white-space:nowrap` row left `.n` almost nothing, so the T6 pair (`정리…` · `등록 해제`) moved into a menu behind one always-visible `⋯`; `＋ 새 작업` stays on the row but only takes layout on hover/`:focus-within`. At rest the row is `name · N runs · ⋯`. The menu opens on **click**, so it is hover-independent — `body.touch` used to only dim the buttons to `.7`, which crowded the name without ever being comfortably tappable. A small self-contained panel, not the board's `.dd` (the cockpit is one framework-free file and does not cross-import): `position:fixed` under the button (flipped up near the viewport edge), `--surface` on `--line-hi`, mono rows that go `--brand` on `--surface2`. Native `<button role="menuitem">`s, so focus and keyboard come for free; Escape or a click outside closes and returns focus; a tree repaint re-points the menu at the new row button instead of closing it (WS deltas must not yank an open menu). **Rail layout only** — every entry calls the same `openNewWork`/`openTidy`/`openUnreg` handler as before. Zero new colors |
| Workspace-header overflow (v5.28 F1) | `.lbl .tmore#wsMore` (mono `⋯`, `data-menu="ws"`, `aria-haspopup`/`aria-expanded`) on the rail's `Workspace` label → the **same** `.rmenu#rowMenu` panel, items `▤ Worktrees` · `↻ Orphans` | **a section header shows its name and one `⋯`.** The label carried three things at once (`▤ worktree`, `↻ 고아 터미널`, `local`) and read as a cramped toolbar; the two maintenance entries moved behind the overflow the repo row already uses, and the machine name left entirely — the top-bar `#mach` chip had been saying it twice. Same panel, same opener path (`paintRowMenu` → fixed position under the button, pulled inward at the viewport edge, `role="menuitem"` buttons, Escape/outside-click closes and returns focus): the second menu is a second **item list**, never a second mechanism, and `[data-more],[data-menu]` is the one selector the close guards trust. `#wsMore` is static markup, so it is wired once like the links it replaced. Behavior is untouched — the items call `openWt`/`openReap` exactly as the old `#wtBtn`/`#reapBtn` did. Zero new colors |
| Shared `⋯` menu (v5.28 G1) | `openMenuAt(btn, acts, repoId)` → `paintRowMenu` → the one `.rmenu#rowMenu` panel; callers are `[data-more]` (repo row) and `[data-menu]` (`ws` · `scratch` · `projects` · `top`) | **`⋯` is many, the panel is one.** Every overflow in the cockpit goes through the same opener — same toggle-on-second-click, same fixed positioning (flipped up at the viewport edge), same `role="menuitem"` buttons with the first one focused, same Escape/outside-click/focusout close returning focus to its `⋯`. What differs between callers is the **item list**, never the mechanism: `ROW_MENU_ACTS`, `WS_MENU_ACTS`, and `SEC_MENU_ACTS` (a `data-menu` → items map). Every item names an `act` that the single `#rowMenu` click handler maps to an **already existing handler** — no menu owns behavior. The two static `⋯` (`#wsMore`, `#topMore`) are wired once; the two that live in the repainted tree (`scratch`, `projects`) are reached by delegation and, on a repaint, the open menu is re-pointed at the new button instead of being dropped. Zero new colors, zero new components |
| Section-header overflow (v5.28 G2/G3) | `.lbl .tmore` (`data-menu="scratch"` · `data-menu="projects"`) on the tree's two section labels → the shared `.rmenu#rowMenu`; items `＋ Session`·`Tidy…` and `＋ Add project…` | **the shape is now the same everywhere: a header is a name and one `⋯`.** Scratch's two inline `.lnk`s and the bare `Projects` label were the last exceptions to what F1 did to `Workspace`. The items call `openSession`/`openScrub`/`openAddProject` unchanged. Both empty states were re-pointed at the affordance that now carries the action (`열린 세션 없음 — ⋯ 에서 ＋ Session`, `등록된 repo 가 없습니다 — ⋯ 에서 ＋ Add project`) — an empty state that names a control nobody can see is worse than no hint at all, and the Projects one no longer sends the owner to the board for something the cockpit can do. The old `.lbl .lnk` rule left with its last user. Zero new colors |
| Top-bar overflow (v5.28 G4) | header right = `● live` (`#ws`) → `.toggle#topMore` (mono `⋯`, `data-menu="top"`, inside the existing `#apWrap`) → `← Board`; items `∗ Secrets` · `◎ Notifications` | **status and primary navigation stay in the open; actions fold.** `#secretsBtn` and `#attnBtn` left the bar and became two menu items that call `openSecrets()` and `setAttnPop(true)`. The attention popover is **not rebuilt** — same `#attnPop` markup, same checkboxes, same outside-click/Escape/focusout wiring; only its trigger moved, and because `⋯` sits inside `#apWrap` the popover keeps its anchor and its "a click inside the wrapper is not outside" guard for free. Reopening is deferred one tick so the click that chose the item is not read as the outside click that closes what it just opened. `#topMore` inherits the `.toggle.on` accent the old `◎` carried, so *notifications are armed* is still visible without opening anything. `#waitChip` and `#ver` stay put — they are status, not actions. Zero new colors |
| Toast action | optional `act` arg on `toast(msg, {label, run})` → one `.tgo` link-button inside `.toast` | a toast that names the way out must *be* the way out: the file-viewer jail refusal says "설정 → 파일 뷰어 루트에서 넓힐 수 있음", and that phrase is now a click target that opens the board's Settings view (`gotoBoard('settings')` — the existing settings surface, not a new one). At most one action, `--brand` text on a faint brand underline, and the dwell time goes 2.6s → 7s so it can actually be reached. `.toast` is `pointer-events:none`; only `.tgo` re-enables it. Still never a question — the rule that outcomes and errors live here, and decisions live in `confirmUI`, is unchanged |
| Work context (v6.0 W2) | `▤ WORK.md` `.tact` on the work row → the **existing file-viewer pane** (`ensureViewer(path,name,{edit:true})`) on `<dataDir>/work/<taskId>.md` | one markdown file per work — goal, decisions, constraints — injected into every agent launched or steered under it. **A mono `▤`, never an emoji** (the cockpit's glyph rule; the spec's 📝 is shorthand). No new file surface: the doc lives under `~/.coxpit`, which is inside the viewer's default root, so `/api/fs/read`·`/api/fs/write` already reach it; `POST /api/tasks/:id/work` only creates the empty file on first open and says whether that root covers it (`inRoot:false` → the same pane reads/writes through `GET`/`PUT /api/tasks/:id/work` instead — the affordance bends, it does not break). `md` viewer panes gained the same `편집` button the text panes already had, so preview ↔ edit happens in place. The row's `title` states the contract plainly — **saved edits reach the next launch/steer, not a running turn** — because a context file that silently misses the turn you are watching is worse than none |
| Add-project picker (v5.28 G5) | Projects `⋯` > `＋ Add project…` → `.modal#projModal` — the `.pick` shell (`.pick-h ＋ Add project` · `.pick-path#projPath` current folder · `.sheet-note` · `.pick-list#projList` of `.pick-row`s · `.sheet-err#projErr` · `.pick-f` with `⌂ home` · `#projHint` · `#projRegHere`) | **a project can be born in the cockpit.** The Projects empty state used to send the owner to the board for the one thing the tree could not do; this is the board's repo browser (`openBrowse`) in the cockpit's own sheet shell — same three endpoints, no new transport: `GET /api/browse` to navigate, `POST /api/repos` (Register) and `POST /api/repos/new` (Start here, greenfield) on the rail's `machineSlug()`. **Pick, never type**: a row click descends, a `git` row carries a `git` `.rp` chip + a `Register` `.pbtn`, an **empty** row carries `Start here`, and a folder with files in it carries neither — greenfield touches nothing that exists. `.pick-row .pbtn` is the same icon-ghost treatment as `.tact` (transparent, `--muted` on `--line-hi`, `--brand` on `--surface2`) — never a filled button inside a list. Success closes the sheet, `hydrate()` repaints the tree with the new project, and a toast names it; failures print **inline** in `.sheet-err` (the add-agent 409 rule), and the `NO_COMMITS` refusal answers the question it raises by pointing at `Start here` |
| New-work sheet | `.modal#workModal` — the `.pick` shell (`.pick-h` · `.pick-path` repo line · `.sheet-note` · `.pick-f` with `.pick-name` + `.go`) | reuses the session-name input pattern verbatim, so naming a work feels like naming a session. Creating it opens a **`main`** terminal at the repo checkout (D2) — the note says so in one line, because "새 작업" that silently makes a worktree would be a lie. Enter submits; backdrop/× closes |
| Add-agent sheet | `.modal#agentModal` — `.pick` shell + `.sheet-body` (`.flabel` over each control: role `.pick-name` · provider `.modes`/`.mode` seg from `/api/fleet` providers · model `.pick-name` · **place `.modes`/`.mode` seg `#agentPlace`** + `.sheet-tradeoff` + `.sheet-err`) + `.pick-f` (`.rchk` real · `#agentPlaceHint` · `.go`) | one agent, one role, under this work. Provider is the existing cockpit `.modes` seg, model is a free-text input (CLI model names churn — no hardcoded list, empty = CLI default), `real` is the same dry/real checkbox as the request bar. **Place (v6.0 P4) is a third `.modes` seg — `worktree`(default, today's muscle memory) │ `in-place`** — with one mono `.sheet-tradeoff` line under it stating the cost of each (`in-place = 체크아웃 공유, 순차 · worktree = 병렬, 나중에 머지`): the tool never implies you get both. The footer hint (`#agentPlaceHint`) swaps with the choice, so placement is never implicit. `.sheet-err` is the **inline** failure line (`--failed`, no new color): the `IN_PLACE_BUSY` 409 keeps the sheet open and prints *steer it, or launch in a worktree* where the choice was made — a toast that vanishes would hide the one thing the human must act on |
| In-place marker | the run row's existing `.meta` reads `r12 · in-place` (tree) + `in-place` on the Review compare merge button (disabled) | an in-place run has **no branch chip, because it has no branch** — its edits land in the checkout. Nothing new is drawn: the id line gains three words and the merge affordance states why it is off (`체크아웃에서 직접 작업 — 머지할 것이 없습니다`), matching the server, which refuses merge/PR/sync on the same root-session marker. The compare diff below it is the checkout's uncommitted changes, said plainly in the button title |
| Scratch section (v6.0 S1) | the tree's first section — `.lbl` **Scratch** + `.lbl .lacts` holding **one `.tmore` `⋯`** (v5.28 G2; the two actions `＋ Session` · `Tidy…` moved into its menu, English since F2, the descriptive Korean in the `title`), then the `.tnode.session` rows | the sessions bucket, renamed to what it actually is: *프로젝트가 되기 전의 생각들* — ideas before they're projects. **Data is untouched** (`kind='sessions'`); this is a label + affordance change. The label is now a name and one `⋯`, the same shape as `Workspace` and `Projects` — the two inline `.lnk`s that used to ride it are the menu's item list, calling the same `openSession`/`openScrub`. The `.lacts` flex group stays (`justify-content:space-between` with three children would fling them to both ends — the same failure the `.tact+.tact` rule fixes on work rows), and the empty state points at the `⋯` rather than at a control that is no longer on the row |
| Scratch cleanup sheet (v6.0 S1b) | `.modal#scrubModal` — `.pick` shell + `.sheet-note` + `.pick-list#scrubList` of `.scrub-row`s (native checkbox · `.snm` name · `.slive` liveness · `.sage` `rN · N일 전`) + a `.scrub-fold` row + `.pick-f` (`#scrubHint` count · `#scrubGo`) | the list must be cleanable **from where it clutters**. Liveness and age come from the `/api/fleet` data the client already holds (`tmuxWindow`+`status` = is the terminal alive · `startedAt`/`endedAt` = when it last moved) — **no N+1 endpoint**. Deletion runs one pass through the **existing** session-delete path (`DELETE /api/runs/:id`), and the note says the thing that must never be in doubt: **the folder and its files are always preserved**. Two selection rules, stated in the sheet: only terminal-less sessions are pre-checked, and sessions quiet for **14+ days fold under `오래된 세션 N`** — folding is a scannability device, so nothing behind the fold is preselected or deleted (a sheet that deletes what you cannot see is a trap, not a broom). `.slive` is `--done`/`--faint`, `.scrub-fold` is muted — **zero new colors** |
| Promotion sheet (v6.0 S2) | `.tnode.session .pr` (mono `⇧`, hover-revealed like `×`) → `.modal#promoModal` — `.pick-path` folder line + `.modes#promoMode` seg (**프로젝트로 등록 │ 프로젝트로 이동**) + `#promoWhat` `.sheet-tradeoff` + `.sheet-err#promoErr` + `.pick-list#promoRepos` (`.pick-row.on` ◉/◯ radio rows) + `#promoWarn` in the footer | an idea becomes a project **without losing its terminal**. *등록* reuses the existing Register flow (`POST /api/repos`, path prefilled from the session folder; an already-registered path is reused, never registered twice) and then re-parents; *이동* picks a registered repo and re-parents via `PATCH /api/tasks/:id {repoId}`. The run is never touched — `worktreePath` stays, so the same tmux session keeps running in the same folder, and the sheet says so. When the folder lies **outside** the chosen repo, `#promoWarn` (amber `--blocked`, the existing caution wash) states it plainly and **still allows it**: what changes is organization, not the terminal. Failures print inline in `.sheet-err`, the same rule as the add-agent 409 — a toast that vanishes hides the one thing the human must act on |
| Project hygiene (v6.0 T6) | two more `.tact`s on the `.tnode.repo` row — `정리…` (`data-tidy`) and `등록 해제` (`data-unreg`), siblings of the existing `＋ 새 작업` — the second opening `.modal#unregModal` (`.pick` shell + `.pick-path` repo line + `.sheet-note` + a `.sheet-body > .sheet-tradeoff#unregRisk` aggregate + `.sheet-err#unregErr`) | **the clutter must be cleanable from where it lives.** A project row that can only be created and never unmade grows a list nobody trusts. Unregister reuses `DELETE /api/repos/:id` and the sheet says the one thing that must never be in doubt: **registration only — the folder and its files on disk are untouched, and re-registering is one click**. `#unregRisk` aggregates the v4.1 close-guard over the repo's still-open works *before* you press (`N open works · M with unmerged output`), because the server refuses while any work is open (409) and being told after the refusal is being told too late — the line points at `정리…` as the way through. The failure prints **inline** in `.sheet-err`, the same rule as the add-agent 409. Zero new colors, zero new components |
| Stale-work cleanup sheet (v6.0 T6) | `.modal#tidyModal` — `.pick` shell + `.sheet-note` + `.pick-list#tidyList` of `.scrub-row`s (native checkbox · `.snm` title · `.swarn` unmerged aggregate · `.sage` `#id · run N`) + `.sheet-err#tidyErr` + `.pick-f` (`#tidyHint` count · `#tidyGo`) | the project-side twin of the Scratch cleanup sheet (S1b), and it reuses that sheet's row shape verbatim — one broom, one look. Only **settled** works are listed (no run in `running`/`preparing`/`pending`/`open`), so a live terminal is never offered for closing, and the note states what closing costs: **the run worktrees go, the repo checkout and its files stay**. Unmerged output is shown **once, as a column in the table** (`.swarn`, `--blocked` — the existing caution color, never a new one) and those rows are **never pre-checked**; safe rows are. Closing is one pass through the existing `POST /api/tasks/:id/close`, and a server 409 does not spawn N dialogs: every `atRisk` is aggregated into the sheet's own `.sheet-err` line and the footer button becomes `그래도 닫기 (N)` (the v4.1 group-close rule, moved into a sheet). Changing any checkbox cancels that armed state — force is never inherited from a decision you have since edited |
| Orphan terminal reaper (v6.0 T6) | rail `Workspace` `.lbl` → `.tmore#wsMore` ⋯ menu > `↻ Orphans` (v5.28 F1; was the inline `.lnk#reapBtn`) → `.modal#reapModal` — `.pick` shell + `.sheet-note` + `.pick-list#reapList` of `.scrub-row`s (checkbox · `.snm` session name · `.slive.dead` `빈 셸` **or** `.swarn` `돌고 있음: <command>` · `.sage` `rN · run 기록 없음`) + `.pick-f` (`#reapHint` · `#reapGo`) | the cockpit's half of the **maintenance family** the board opens with `Reclaim worktrees` (`#i-recycle`); the cockpit has no icon sprite, so the affordance wears the mono `↻` it already uses for refresh — never a colored recycle emoji. Fed by `GET /api/tmux/orphans`, which lists only `coxpit-r<N>` tmux sessions whose **run id has no record in the DB** — a live run's session is not filtered out in the client, it is *never offered*, and `POST /api/tmux/orphans/kill` re-derives that same list server-side before killing (`tmux kill-session -t '=<name>'`, exact match — `coxpit-r5` must never take `coxpit-r50`). Selection follows the S1b rule: **idle shells are pre-checked, a pane running anything else is flagged with its command and never pre-checked**. Both maintenance entries now ride the `Workspace` label's single `⋯` (v5.28 F1) — the label itself is back to a name, and the sheet title reads `↻ Orphans` like the item that opens it. Zero new colors — `--blocked` for the flag, `--faint` for the idle line |
| Worktree reclaim sheet + disk readout (v6.0 T6b) | rail `Workspace` `.lbl` → `.tmore#wsMore` ⋯ menu > `▤ Worktrees` (v5.28 F1, sibling of `↻ Orphans`; was the inline `.lnk#wtBtn`) → `.modal#wtModal` (title `▤ Reclaim worktrees`) — `.pick` shell + **`.pick-path#wtTotal` one-line readout** (`worktree N개 · XMB · 그중 미머지 M개`) + `.sheet-note` + `.pick-list#wtList` of `.scrub-row`s (checkbox · `.snm` `rN · <branch>` · `.swarn` `미머지 — 유일한 사본` when `reclaimRisk` · `.sage` `<reason> · XMB`) + `.pick-f` (`#wtHint` selected count/size · `#wtGo`) | **you cannot manage what you cannot see.** The debt was invisible until the disk filled, so the sheet opens with the number (`#wtTotal`, the `docker system df` line) before it offers a broom; the same figure rides `GET /api/health` as `worktrees:{count,sizeKb}` for anything watching from outside. Fed by the existing `GET /api/worktrees` — no new endpoint for the readout, `totalKb`/`sizeKb` were already there. Rows reuse the reaper's shape verbatim (third member of the same maintenance family), and selection follows the same rule one more time: **safe rows (merged, or exported/PR'd) are pre-checked; a row whose changes are unmerged AND un-exported is shown, flagged `--blocked`, and never pre-checked** — that worktree is the only copy of that work. The sheet always posts explicit `runIds`, so nothing is reclaimed that the human did not tick, and the server refuses to include a flagged item in a bare prune-all regardless of what any client sends. Zero new colors, zero new components |
| Tab state dot (v5.28 A4) | `.as-dot` + `data-role="asdot"` on every terminal tab (`kind!=='viewer'`), after the name | the tab's **second** axis: the leading `.st` dot is the run's status, this one is what the agent's terminal is doing right now — `working` `var(--running)` · `waiting` `var(--blocked)` (pulses, `@keyframes aspulse`, off under `prefers-reduced-motion`) · `idle` `var(--faint)` · `exited` `var(--done)`. **All existing tokens — zero new colors.** A run with no live agent state gets no class and the dot is `display:none`, so untracked tabs look exactly as before. Painted from the server's `agentstate` stream only (`paintAgentState`) — the client never guesses a state or synthesizes a transition |
| Tree-dot agentstate override | the same four `.as-*` classes appended to a tree run/session row's existing `.st` dot | **the tree is the rail** (v5.28 A4 revised): a separate Agents rail would duplicate the v6.0 Project ▸ Work ▸ Session tree, so agent state paints the run rows the tree already draws. The `.as-*` rules sit *after* the `.st.*` status rules (same specificity → later wins) and clear the running ring. Rows without agent state keep today's run-status coloring untouched, and **tree order never changes** — no attention re-sorting: the tree navigates, the chip calls |
| Waiting chip | `#waitChip` (`◔ N`, `#waitN`) in the cockpit header | the ambient glance: how many agents are waiting on you, shown **only when N>0**. Clicking focuses the next waiting terminal, cycling through them (already-open tab → focus its pane; otherwise opened as a tab). Lives in `<header>`, so it survives focus mode (`.layout.focusmode` hides only `.rail`/`.reqbar`) and the ≤860px header — attention must outlive the chrome it rode in on. Amber like every other blocked affordance (`--blocked` on the `.wip`/`.chip.blocked` wash), never a fresh color |
| Attention popover (v5.28 A5) | the top-bar `⋯` > `◎ Notifications` (v5.28 G4; was the inline `#attnBtn`) → `.apwrap` > `.apop` anchored panel | the cockpit has **no settings surface** and does not grow one for three preferences — they live in a small popover off one header button, next to `#waitChip`. Three rows, all **existing components**: `소리` and `브라우저 알림` are `.rchk` checkboxes, `울릴 전이` (대기만 │ 완료만 │ 둘 다) is the `.modes`/`.mode` seg, plus an `.apop-note` line stating the contract ("전부 기본 꺼짐. 보고 있는 탭은 자신을 울리지 않습니다"). Persisted in `localStorage` as `coxpit.sound` · `coxpit.notify` · `coxpit.pingOn`, **all off/both by default**. Enabling notify runs `Notification.requestPermission()` and falls back to off + an error toast on denial (the browser's permission is the SSOT — a remembered "on" without permission reads as off). `.toggle.on` tints the header `⋯` brand while anything is on. Esc or focus leaving the wrapper closes it; focus is visible via the global `:focus-visible` ring. Panel is `--surface` on `--line-hi` — **zero new colors** |
| Attention delivery | `raiseAttention(runId, prev, next)` inside `paintAgentState` | the audible half of the v5.28 A4 dots. Only a transition **into** `waiting`/`exited`, only when `document.hidden` or the run isn't the focused tab, only if the `coxpit.pingOn` filter admits it — the focused tab never pings itself, and the dots/`◔` chip stay always-on regardless of prefs. **Sound** is a two-tone WebAudio blip generated in code (no audio asset, no new file), quiet on purpose. **Notification** body is `<세션 이름> · 입력 대기` / `· 완료` — the run's `runLabel()` and the state, **never terminal content**; clicking it focuses that tab through the same `openTab` jump the chip uses. Server twin: `src/agentstate.ts` POSTs `{event:'agentstate', runId, state, url?}` to `COXPIT_WEBHOOK_URL` on the same transitions, **state only** (the endpoint is outside the trust boundary) with a 60s per-run cooldown |
| Listener panel (v5.28 B) | `.modal#portsModal` — `.pick` shell + `.pick-path#portsWhere` (pane pwd · machine) + `.sheet-note` + `.port-ask` row (`.pick-name#portQ` port field · `#portGo` · `.port-spot#portSpot` one-click chips) + `.pick-list#portsList` of `.port-row`s + `.sheet-err#portsErr` + `.pick-f` (`#portsHint` · `#portsRescan`) | **evidence, never a verdict.** The panel answers one question — *what is listening here, and since when* — and refuses the next one: there is no "stale" badge, no score, no auto-kill. A row is `:PORT · command · started <etime> · pid N`, and the human reads "started 3h ago" next to "you edited 2m ago" and draws their own conclusion. Two entry points, one panel: the per-pane `:ports` affordance and the ⌘K row *포트에 뭐가 떠 있나…*. Backed by read-only `lsof`+`ps` (`src/procscan.ts`) through `GET /api/runs/:id/listeners` (pwd from the §D-fix `getRunPwd`) and `GET /api/machines/:id/port/:port`; a machine without `lsof` returns an honest empty list and the reason prints in `.sheet-err`. **Zero new colors** — the port is `--brand`, everything else `--muted`/`--faint`, and the only accent is the one borrowed marker below |
| Listener row affordances (v5.28 B) | `.port-row` > `.swarn` `⌖ 여기서 실행` marker + `.pkill` `[종료]` button | the marker is **T6b's `미머지` treatment, reused verbatim**: a mono glyph plus a word in the existing caution token (`--blocked`), never a fresh color and never a colored badge — it states one correlated fact (this listener's cwd/exec is under the pane's checkout) and claims nothing beyond it. `[종료]` is the only action on the row: it confirms with the row's own evidence in the prompt, posts `{pid}` to `POST /api/machines/:id/kill` (TERM → short grace → KILL, that machine only) and then **re-runs the scan**, so the row disappears or the toast says *아직 살아 있습니다 — 무언가가 다시 띄웠을 수 있어요*. Guards are **server-side**: pid 1, the daemon's own pid and any pid absent from a fresh re-scan are refused, so this is not an arbitrary-pid kill endpoint. There is deliberately **no "kill everything on this port"** — it would take an unrelated process with it. `.pkill` is a ghost control (`--muted` on `--line`) that only reddens to `--failed` on hover |
| Pane `:ports` affordance (v5.28 B1) | `.leaf-h .pact.ptxt` (`data-ports`, mono `:ports`) next to the pane's `⧉`/`↺` actions | a word instead of a glyph, at the same ghost weight — the leaf header's actions are already dense and a new icon would have to be invented. Asks the pane's own question (*what did **this** checkout leave running*), so it carries the run id and resolves against the pane's live pwd. Passive port spotting rides Part A's existing output tail (`spottedPorts` in `src/agentstate.ts` — **no second tap**): a high-confidence `listening on :PORT` / `http://host:PORT` line becomes a `.port-spot` chip, so a spotted port is one click. Conservative patterns only, same honesty rule as A2 — a port is never invented |
| Quick-reply strip (v5.28 C1) | `.leaf-h .qr` (`data-role="qr"`, `data-qrun`) holding `.qbtn` pills — `승인` · `거절` · `계속`, an optional saved reply, and a `.qbtn.qadd` `+` | **coxpit makes the common reply one tap; it never answers the prompt for you.** Each button sends a **fixed, human-chosen literal** (`QUICK_REPLIES`, plus the trailing `QR_CR` = `\r`) down the pane input channel — the same `{t:'i'}` message `termSendRaw` uses, **no new transport**. There is no code path that reads the agent's prompt to pick an answer: `승인` always sends the same token, and Part A's `waiting` only decides *where the strip appears*, never *what it says*. The strip is rendered by `paneStripHTML(runId)` off the agentstate map and repainted by `paintPaneStrip` inside `paintAgentState`, so it vanishes the moment the pane leaves `waiting` (`.qr:empty{display:none}` — the same "draw nothing when there is nothing" rule as `.as-dot`). Click-opened and keyboard-reachable, **never hover-gated** (mobile has no hover). `+` swaps itself for the existing `.sendkey` inline input (the secret-send gesture, not a native `prompt`) and remembers one custom reply **per machine** in `localStorage('coxpit.quickreply')` — a device preference, not a repo setting. Colors: the caution token `var(--blocked)` Part A already uses for `waiting`, on `--line-hi`. **Zero new colors** |
| Start / resume strip (v5.28 C2) | the same `.leaf-h .qr` slot, filled with `.sbtn` pills — `에이전트 시작` (`data-astart`) and `이어서` (`data-aresume`) | shown only when the pane has **no live agent** — Part A state `idle`/`exited` **and** a run that is not `running`/`pending`/`preparing` (read from the existing state, never recomputed). Both are buttons over **paths that already exist**: `시작` posts to `POST /api/tasks/:id/run` (the request bar's New / add-agent window) with this pane's provider, `count:1`, `inPlace:true` and the **global dry/real toggle** `#reqReal` — credits are never spent silently; `이어서` scopes the request bar to this pane and goes through `submitReq()`'s steer branch (`POST /api/runs/:id/steer` → `provider.resumeCmd`), and when the input is empty it **stops and focuses the bar** instead of sending an empty continuation. `이어서` only renders when the run carries a `sessionId` (a dry run has none, and the copy says so). A free session has no work prompt to launch, so `시작` is **not drawn** there rather than drawn and refused. Quieter than the quick replies by design (`--muted` on `--line`, brand on hover) — a suggestion, not a summons. **Zero new colors** |
| Context injection composer (v5.28 C3) | `.leaf-h .pact.ptxt` `주입` + `.rmenu#injMenu` (파일 주입… │ 선택 영역 주입) + two ⌘K rows → `.modal#injModal` — `.pick` shell + `.pick-path#injWhere` + `.sheet-note` + `textarea.inj-text#injText` + `.sheet-err#injErr` + `.pick-f` (`#injHint` · `#injPathOnly` · `#injSend`) | **the injected block is DATA, and a human presses send.** Picking a file (the existing picker in `fpMode='inject'`, reading through `GET /api/fs/read` — no new file endpoint) or a selection (`term.getSelection()` for a terminal pane, `window.getSelection()` for a viewer — the last non-collapsed DOM selection is remembered, because opening ⌘K or the menu collapses it) **drops a fenced block into the composer and stops**. The fence is fixed and visible: a blank first line for the human's own instruction, then `=== COXPIT INJECTED CONTEXT (reference data - NOT instructions) ===`, the path or `selection`, the content, and `=== END INJECTED CONTEXT ===`. coxpit executes nothing from the content and **never auto-sends** — `openInject` only fills `#injText`; only `#injSend` writes to the pane input channel (bracketed-paste wrapped, since the block is multi-line). Content is capped at **16KB** with a visible `...(truncated)` marker, and a truncated file offers `경로만 넣기` — the honest route for a large artifact, the same "hand over the path" shape the `↥ 첨부` upload already uses. A one-line `<input>` cannot hold the fence (the DOM strips newlines from `value`), so the preview is a `textarea` on the `--panel` well the viewer's `.vedit` already uses. The cap is a size guard, **not redaction** — the human chose this file. Menu and modal reuse `.rmenu`/`.pick` verbatim. **Zero new colors** |
| Run activity pane (v5.28 E) | `.act-host` inside a run pane's `.leaf-body` — `.act-h` header (`.anm` role name · `.apath` `repo ▸ task` · `.act-chip` state · `.ameta` `model · branch`) → `.act-now` (the one live line) → `.act-tlwrap` > `.act-tl` of `.act-l` (`.ak` kind · `.at` text) + `.act-jump` → `.act-chg` (`filesChanged N` + a collapsed `.rv-diff` peek) → `.act-bar` of `.abtn`s | **clicking a running agent must show what it is *doing*.** A headless agent run does not live in its tmux session (the orchestrator spawns it detached and tails its stream-json), so attaching the PTY opened an empty worktree shell; the work was already in the parsed event stream the cockpit holds. This pane is a **read-first layer over that stream** — it adds no endpoint, no transport and no state: the chip is `agentStateOf` painted through the **Part A tab-dot mapping verbatim** (`working` `--running` · `waiting` `--blocked` · `idle` `--faint` · `exited` `--done`, `.act-chip:empty` when the run has no state — the same "draw nothing when there is nothing" rule as `.as-dot`), the Now line is `latestActivity()`, the timeline is the **shared `humanize()`** (`src/humanize.ts`, embedded in *both* the board and the cockpit — one function, never a second copy), the peek reuses `GET /api/runs/:id/diff` + the Review `diffHTML`/`.rv-ws` whitespace toggle (one shared preference), and the action bar reuses the request bar's steer path and Part C's quick-reply strip (`.qr`, only while `waiting`). New lines **append at the bottom** and auto-scroll only while pinned to the tail; scrolled up, following stops and `.act-jump` offers the way back. Honesty: an empty stream reads `starting…` — never an invented activity — and `Review` goes to the board's own run detail through the existing `/?run=N` deep link rather than redrawing it here. **Zero new colors, zero new endpoints** |
| Activity \| Terminal toggle (v5.28 E1) | the existing `.modes`/`.mode` seg, sized down for the leaf header (`.leaf-h .modes{padding:1px}` · `.leaf-h .mode{font-size:9.5px}`), `data-pmode`/`data-prun`; twinned by the `Open terminal` `.abtn.prim` in the action bar | tabs are keyed by runId, so a run cannot be two tabs — the run tab gets a **mode** instead. An **agent run** opens in `activity` and its raw PTY is built **lazily**, only when the mode first flips to `terminal` (`ensureTab` → `ensureTerm`); a **hand session or workbench** (`runIsSession`, or `agent==='session'`/`'workbench'`) defaults to `terminal` exactly as before and gets **no toggle at all** — those have a real shell, so the terminal is the right view and an empty "activity" view would be a lie. Once built the terminal is **retained** (switching back is instant, scrollback intact) and nothing about today's attach behaviour changes: `/ws/term/:id`, resize, copy, links and the input bar are untouched. The seg is the only new chrome, and it is the one the request bar already uses |
| Board entries (v6.0 B) | three `k:'board'` rows in the cockpit ⌘K palette — **Board** (`/`) · **Archive** (`/?view=archive`) · **Workrooms** (`/?view=goals`) — routed through one `gotoBoard(view)` | **the cockpit is the home; the board is the reading room** — one keystroke away, never the front door. The palette is the only new affordance: the header's `← Board` toggle is unchanged and **no board route, view or feature is removed** (a fleet-first user loses nothing). The rows reuse the existing `.pal-row` shape (`.pk` kind · `.pl` label · `.ph` hint) and carry the destination in `.ph`, so where you land is readable before you press Enter — **zero new colors, zero new components**. Board side: `?view=` wakes the board's own `setView()` through the existing `/?run=N` deep-link block (whitelisted, then stripped from the URL) — the mechanism is reused, not invented |
| Dry run badge (v5.28 H) | `.dryc` — a lowercase mono **`dry`** chip drawn by `dryChip(run)` (one copy in `board.ts`, one in `cockpit.ts`) on the cockpit tree run row (beside the `.st` dot), the activity-pane `.act-h` header, the board run card header and both compare columns; the merge guard is `confirmDryMerge(n)` over the existing `confirmUI` | **a mock must never be mistaken for real work.** dry/real was never stored, so a dry run sat in a live 3-way race indistinguishable from real candidates and could be picked as the winner — the only tell was opening the diff and finding `COXPIT_DRYRUN.txt`. Now `agent_runs.real` (NOT NULL **DEFAULT 1**) carries the truth and rides every serialized run (`/api/fleet` · `/api/runs/:id` · `/api/tasks/:id` · `/api/tasks/:id/compare` · `/api/groups/:id`) and every `{type:'run'}` WS delta. **Only a known dry run is badged** — the default is `real` on purpose, so pre-migration and not-yet-hydrated runs are never falsely accused (`isDryRun(r)` is exactly `r.real===false`, never a falsy check). A real run gets **no** chip: real is the norm, only the mock is called out. Merging or picking a dry run raises `confirmDryMerge` first (compare · workroom · the bare `Integrate selected` fan-in, which counts the dry ones), never native `confirm()`. The cockpit had no confirm dialog of its own, so it gained the same promise-based `confirmUI(msg,{title,sub,okLabel})` contract the board has, built from the **existing** `.pick` sheet shell (`.pick-h` · `.sheet-note` · `.sheet-tradeoff` · `.pick-f` `.home`/`.go`) — a second caller of one component, not a second mechanism. The word is a **word**, never an emoji, and the chip borrows `--faint` text on a `--line-hi` border: **zero new colors** |
| Board session exclusion (v5.28 I) | no component — a client-side guard in `board.ts`: `sessionRepoIds` (from the fleet's `repo.kind === 'sessions'`) + `isSessionTask(id)` / `isSessionRun(run)`, applied at every point a repo/task/run enters the board's state (hydrate · `upsertRun` · the `run`/`task` WS deltas · single-run and archive fetches) | **the board is the reading room for agent work — a free terminal session is not a run, so it never shows here, though it still lives in the cockpit.** Every run used to appear on the board including Scratch sessions, which are free shells in the virtual `sessions` bucket, not agent runs; among real candidates they were noise. The filter is **board-only**: `/api/fleet` is unchanged (the cockpit reads the same payload and needs its Scratch section), no endpoint and no schema moved. A session repo never renders in `#repoList`, is never counted in the rail's active-run badges, is excluded from the *All repositories* scope and from the archive list (archive rows carry no `repoId`, so they are matched by the bucket's name), and a session started while the board is open is stopped at the delta rather than popping in. Nothing new is drawn: **zero new colors, zero new components** |
| Post-merge verify line (v5.28 J) | the cockpit Review column's **existing `.rv-verify` row** (`data-role="mverify"`, the `vbadge(status)` chip + a clickable `.vout` tail) and the board compare column's `.cmp-mv` line, both drawn from `mergeVerifyById`/`mergeVerifyByRun` — what the merge response itself carried | **a winner that merges should be checked the same breath it lands.** `verifyRun` only ever verified a run's *worktree at settle* (pre-merge), so the merged result on the base went unchecked and the owner hand-built a "smoke test" task every ticket. `POST /api/runs/:id/merge` now answers `{ ok, detail, verify:{ status, output } }` — `verifyBase(repoId)` running the repo's **own** `verifyCmd` at `repo.path`, the default branch the merge just landed on, through the **same `runVerifyCmd` runner and pass/fail parse** `verifyRun` uses (one runner, two targets — never two verdict rules). The same block rides each merged entry of `POST /api/integrate`. Honesty, in three parts: it runs **only when `verifyCmd` is set** (a command is never guessed), it runs on the **merged base** (never a stale worktree), and a `fail` **never un-merges** — `status:'merged'` stands and the line reports, with `머지는 됐지만 검증 실패 — 로그 확인` and the tail one click away in the existing verify-output toast. With no `verifyCmd` the merge site says so instead of going quiet: `검증 명령이 없어요 — repo 설정에서 verifyCmd 를 지정하면 승자를 자동 검증합니다`, pointing at the **existing** setter (the cockpit's `#rvVcmd` → `PATCH /api/repos/:id`; the board links across to it). The cockpit reuses the verify row verbatim, and the board's one new rule (`.cmp-mv`) exists only because `.cmp-f .msg`/`.cmp-meta` are `nowrap` and a truncated nudge is not a nudge — it takes `--surface2`/`--line`/`--muted` and the **status** tokens (`pass` → `--s-done`, `fail`/`error` → `--s-failed`) it already had. **Zero new colors, zero new transport, zero schema** |
| HUD pill (v5.28 K3) | `/hud` (`src/hud.ts`) — `button.pill#pill` holding `.pgrip` (the drag grip) · `.pdots` (one `.st` per live agent, capped at 3+3) · `.pwait` (`◔ N`, `--blocked`) · `.pquiet` (`N running` / `quiet`, `--faint`) | **the glance, and at rest it is all there is.** The HUD is the cockpit's own fleet re-surfaced small, so it draws no state of its own: the dots are the **Part A tab-dot mapping verbatim** (`working` `--running` · `waiting` `--blocked` pulsing · `idle` `--faint` · `exited` `--done`, the same `.as-*` classes and the same `@keyframes aspulse` respecting `prefers-reduced-motion`), and the count is `◔ N` — the `#waitChip` glyph, not a new one. **No labels** (the mockup's annotations were annotations), no icon sprite (the page is one self-contained string like `board.ts`/`cockpit.ts`, so it wears mono glyphs), and **no pulse when nothing waits** — a quiet fleet reads `quiet`, because a pill that nags in quiet times is a pill you park off-screen. **A click (or `enter`/`space`) expands it** — and that one sentence decides the pill's drag story, because in a frameless window a drag region *swallows* the click (the window moves instead, and `expand()` never runs). So the pill and its spans are `-webkit-app-region:no-drag` and the window is dragged by a single `.pgrip` handle (`⠿`, `--faint`, `cursor:grab`) at the leading edge: the drag region and the click target cannot be the same element, and the click wins the pill because you open the HUD far more often than you move it. The three sizes the desktop window will later want are written to **`<html data-hud="pill\|list\|detail">`** and nothing else: this run resizes nothing — the page just re-lays-out from that attribute, so `/hud` works standing alone in a browser and the desktop shell (K1/K2: the floating window, the global hotkey) reads the same attribute when it lands. **Sized to be hit** (v6.4, raised again v6.3.9): the pill is `32px` tall with `13px` mono type — a pill you have to aim at is a pill you stop using, and at 26px/11px this one was both hard to hit and hard to read. The owner's display is **3840x2160 at 1x**, where every later size decision on this page gets measured. **Zero new colors** |
| HUD triage list (v5.28 K4) | `.panel` > `.hh` header (`.lead` `N needs you`/`all clear` + `.subs` `· M running · K idle` + `.hx` collapse) > `.hlist` of `.lbl` sections and `.row`s (`.st` dot · `.rn` name · `.rp` path-or-activity · `.dryc` · `.rel` elapsed) > `.cnt` fold + `.hfoot` key legend | **needs-you leads, running folds, the rest is a number.** The order is the point: the reason this surface exists is to be pulled back only when an agent needs you (Part A), so `waiting` rows sit on top in `--blocked` with the pulsing dot, `running` is a slim row that says what it is doing right now (the **shared** `actNowText`/`latestActivity`, `src/activity.ts` — one copy, injected into both the cockpit and the HUD) and carries the `dry` chip (Part H `real===false`, never a falsy check), and idle/sessions collapse to one `▸ idle / session N` line that opens in place — the count is the default because the HUD is 250px wide and a list of things that want nothing is not triage. With **zero waiting** the body is replaced by a calm `all clear` block (the running summary + the idle count), never an empty list: the tone tracks the state. Keyboard is the whole surface — `j`/`k` move, `enter` opens the detail, `y`/`n`/`c` answer the highlighted **waiting** row inline (and refuse, with a toast, on any other row — coxpit never pushes characters at a run that is not at a prompt), `esc` closes one layer. **One line, always** (v6.4): the header is a single row whose summary span ellipsizes in place (`flex:1;min-width:0;nowrap`) with the controls `flex:none`, and because the segments run **waiting → running → idle** the tail that gets cut is the least important count — it used to wrap to two lines the moment anything was waiting, in a 250px window. Rows are `nowrap` end to end: the name shrinks and ellipsizes (`flex:0 1 auto`, not the old `max-width:46%` cap that crowded every narrow width), the path takes the leftover (`flex:1 1 0;min-width:0`) and ellipsizes, the elapsed time is `flex:none` and never pushed out. Type is sized to be read at a glance rather than studied, and **the scale was raised once the owner actually kept it on screen** (v6.3.9): body and rows `14px`, header `14px`, row path `12.5px` / elapsed `12px`, section labels `11px`, detail heading `15px`, buttons and the steer input `13.5px`. The floor moved with it — **nothing below 12px, and the only exception is an uppercase label, which may go to 11px**; e2e parses every `font-size` *per CSS rule* and fails a small size that is not inside a `text-transform:uppercase` block. Rows are `34px` tall with `8px 14px` padding so they are hit targets, not text. The reason the first scale read tiny is not taste: the display is **3840x2160 at 1x**, so 13px is physically small there — a panel you keep up all day has no fine print. **Zero new colors, zero new components** |
| HUD detail pane (v5.28 K5) | `.hdetail` to the **right** of `.hlist` (both visible; `data-hud="detail"`) — `.d-h` (name · `.d-chip` state · `dry`) · `.d-meta` (`repo ▸ task · model · branch`) · `.d-q` waiting card · `.d-now` + `.d-tl` mini timeline · `filesChanged` + `.d-diff` peek · `.d-steer` · `.d-bar` of `.dbtn`s | master-detail, never a replacement: selecting a row **widens** the surface and the list stays beside it, and `esc` closes only the detail. Everything in it is read from data the client already holds or an endpoint that already exists — the timeline is the **shared `humanize()`** (`src/humanize.ts`, one function across board · cockpit · HUD), the change peek is `GET /api/runs/:id/diff`, the actions are `POST /api/runs/:id/stop`, `POST /api/runs/:id/steer` (settled runs only), `DELETE /api/runs/:id` (sessions), `PATCH /api/tasks/:id` (rename) and the `/cockpit?run=N` escape hatch. Three variants because three things get selected: a **waiting agent** (question card + 승인/거절/계속 + steer + open/stop), a **running agent** (no question — the Now line, the timeline, steer, open/stop) and a **session** (`repo.kind==='sessions'`: the terminal tail from `GET /api/runs/:id/scrollback`, 터미널 열기 · 이름변경 · 삭제, and **no** question or steer, because a free shell has no agent to answer). Rename swaps the button for an input (never a native `prompt`) and delete **arms in place** (`삭제` → `정말 삭제? (폴더는 보존)`, disarming after 4s) — the HUD is too small for the board's `confirmUI` overlay, so the consequence rides the button itself and no destructive action ever fires on one click. **Two panes, three wrapping blocks** (v6.4, v6.3.9): detail is a fixed `260px` list column beside a `flex:1;min-width:0` pane, and the only elements on the whole page allowed to fold to a second line are the agent's own words — `.d-q pre`, the session `.d-tail`, and the session's last conversation `.d-chat`, each height-capped (`min(40vh,260px)` / `min(45vh,240px)` / `min(45vh,260px)`) with its own scroll so one long answer cannot make the floating window as tall as the screen. Everything the *app* writes — heading, meta, now line, timeline entries — is a single ellipsized line at the 560px minimum width. **Steer is not drawn until it is known to have somewhere to land** (v6.3.9): `detailHTML` leaves a `data-role="steerslot"` and `loadPane` fills it *after* asking the daemon — `GET /api/runs/:id/scrollback`, which resolves through the same `getRunTermInfo` + tmux that `send-keys` does, so an `ok` there means the input will actually arrive. `run.status` was the wrong witness and produced the bug: it reads `open` for a session with no pane and stays `running` for an agent whose tmux was already cleaned up, so the box appeared, the POST came back `409`, the box was cleared only on success, and the toast was easy to miss — text sat there and nothing happened. With no live pane the slot holds a quiet line instead (*이 세션엔 살아있는 터미널이 없어요* + the `/cockpit?run=N` link): **no input box that silently fails.** When a send does fail, the reason lands **under the input** in `--failed` (`.d-note.bad`) as well as the toast and **the typed text is kept** — it is not something to retype, it is something to resend; only success clears it and shows a brief `보냈어요` in `--brand`. One `Enter`, one send: the detail has a **single delegated `keydown`** that routes by target id (`steerInput` → steer, `renameInput` → commit/cancel), because an element-level handler beside it would fire the same key twice. **Zero new colors** |
| HUD waiting card (v5.28 K5/K7) | `.d-q` — `.qk` source label + `<pre>` content, or `.unk` when there is nothing to show; the `.qbtn` strip (`승인`·`거절`·`계속`) under it | **the question is never fabricated.** coxpit does not read a prompt to decide what is being asked, and it will not paraphrase one: the card shows either the terminal's own last screen (`GET /api/runs/:id/scrollback`, labeled *터미널 마지막 화면* — this is what is on screen, not a parsed question), or the stream's last `said`/`ask` line (labeled *마지막 말*), or it says plainly *무엇을 묻는지 읽지 못했습니다 — 터미널을 열어 확인하세요*. The reply buttons are Part C's contract verbatim: three **fixed, human-chosen literals** (`1`/`2`/`계속`), the same three the cockpit's `.qr` strip sends, and `waiting` decides only *where the strip appears*, never *what it says*. They reach the agent through the one endpoint this part adds — `POST /api/runs/:id/input`, `tmux send-keys` into the live session (the write twin of the read-only `getScrollback`, same exact-session target discipline) — because `steer` resumes a **settled** run and cannot answer a run standing at a prompt. Live run only: no session → `409`, unknown run → `404`. `.qbtn` borrows `--blocked` on `--line-hi`, exactly as the cockpit's does. **Zero new colors** |
| HUD session tail (v5.28 K5) | one read-only body slot (`data-role="sbody"`) in the session variant of the detail, filled with either `.d-chat` (`.d-turn` = `.tk` role + `.tt` pre) or the `.d-tail` `<pre>` | a free session has no event stream, so the honest answer to *what is this?* used to be its last 30 lines of screen. **That answer is empty exactly when it is needed most** (v6.3.9): the panes of most older sessions are gone, `scrollback` returns *no terminal session*, and the owner opened a session to find nothing at all. So the body now falls through three sources, in order of what survives longest: ① the **stored transcript** — `GET /api/runs/:id/chat` (`getSessionChat`, the tagged Claude Code JSONL), rendering the last few turns with the final one shown fuller and the earlier ones collapsed to one trimmed line each; ② the live screen — `GET /api/runs/:id/scrollback?lines=80`, read-only, `--muted` mono on the `--panel` well the viewer panes already use, relabelling the section to *터미널 마지막 화면* so you always know which source you are reading; ③ when there is neither, **a plain sentence and nothing invented** — *이 세션의 대화 기록을 찾지 못했어요 — 세션 태깅(--session-id) 이전에 만들어졌을 수 있어요.*, the same rule as the waiting card. No server route and no `getSessionChat` behavior changed; this is the page choosing what to ask for. Still read-only — a free shell has no agent to steer — and the only actions beside it are the ones that make sense for a shell: open the terminal (cockpit), rename (`PATCH /api/tasks/:id` — sessions are named on the task), delete (`DELETE /api/runs/:id`, whose note repeats the thing that must never be in doubt: **the folder and its files are preserved**). **Zero new colors** |
| HUD desktop window (v5.28 K1/K2) | `desktop/main.cjs` — a **third** `BrowserWindow` (beside the cockpit window and the auth prompt) loading `${boardOrigin}/hud`, plus `desktop/preload-hud.cjs` (`window.coxpitHud.size({state,w,h})` → `ipcMain('hud:size')`, and v6.3.9's `window.coxpitHud.hide()` → `ipcMain('hud:hide')`) and a `globalShortcut` (default `CommandOrControl+Shift+\`, rebindable from the **HUD** menu, mode `ambient`\|`hidden` persisted in `desktop-state.json`) | **the page is the UI; the window is the furniture** — and the two never learn about each other. `/hud` keeps working standing alone in a browser because every desktop affordance is either inert there (`-webkit-app-region:drag` on the expanded `.hh` header and on the pill's `.pgrip` grip **only** — a drag region swallows clicks, so the pill itself and every control stay `no-drag`) or guarded on a bridge that does not exist (`window.coxpitHud` undefined → `reportSize` returns). Conversely the window drives the page **through the keyboard the page already has** (`Enter` expands the pill, `Escape` closes one layer) rather than a second control surface. Size is **the content's to ask for and the person's to overrule** (v6.4 reshape). The first cut hard-coded three window sizes (`262x380` / `582x440`) against a page whose content was half that tall, and the leftover was not empty space but a **transparent window that still swallowed clicks** over whatever sat beneath it — invisible, so undiagnosable. Now the card fills the window (`html,body{height:100%}`, no `max-width` cage, the list scrolling *inside* at `flex:1;min-height:0`) and the page **measures what it wants**: `reportSize` sends `{state,w,h,fit:true}` for list/detail, where `h` is header + what the scroll region actually holds + footer + border — re-sent after every render, every data poll and every late fill (a terminal tail arriving, a fold opening), because following the content is a continuous job, not a one-time one. That measurement walks the children's boxes rather than reading `scrollHeight`, and the reason is the whole difference between a window that fits and one that only inflates: **`scrollHeight` never drops below `clientHeight`**, so once the window had grown, every later report would echo the current height back and the panel could never shrink again. The main process still alone calls `setBounds` (same anchored corner, same `workArea` clamp) but now against **defaults + minimums**: list `340`/min `300x240`, detail `740`/min `560x360` (raised in v6.3.9 with the type scale — at 320/680 an *expanded* panel still read small on a 4K display driven at 1x), never taller than **70% of the display's `workArea`**. `setResizable` is toggled per state — the pill is locked to its measured size (`setMinimumSize` == `setMaximumSize`), list and detail are the owner's to drag — and a manual resize is persisted to `hud.size[state]` in `desktop-state.json`, where it **wins over `fit` from then on**. The one rule that keeps this honest: a size we set ourselves is never remembered (`hudSizing` brackets our own `setBounds`), because persisting our own first `fit` would freeze the window at that height forever and undo the whole thing. The pill still reports a **measured** width because its dot count changes it. It is `frame:false` + `transparent:true` + `alwaysOnTop('floating')` + **`hasShadow:false`** (the OS paints a square shadow outside a transparent window, which turns the rounded pill into a rectangle) and appears with **`showInactive()`** — ambient presence must never pull focus off the frontmost app; only a user summon (`hud.focus()`) does. Position is remembered **per display** (`screen`, keyed by display id, restored on the display under the cursor, offset below the notch by `workArea` + 8px) because multi-monitor is the real case. **A panel that lives on top of everything needs a way out that is visible** (v6.3.9). The owner asked "how do I turn this off?" — and the honest answer was *a hotkey you have to already know*, which for an always-on-top window is no answer. So the header carries **two controls, not one**: `−` still collapses to the pill (unchanged, `esc` too), and a new `×` **hides the widget** — one more one-way bridge word (`hide()` → `ipcMain('hud:hide')`, guarded to the HUD's own `webContents` like `hud:size`) that sets `hud.mode='hidden'` and hides the window through the existing `setHudMode`, so the menu radio follows and the pill does not quietly return on the next launch. The way back is **the hotkey that already exists** (`summonHud`) — no second window, no second channel, and in a plain browser the button is inert (no bridge → a toast saying so, never a dead click). Two things it must keep: **closing the main cockpit window does not kill the HUD** (`window-all-closed` quits only when neither window is alive, and `activate` judges by the *main* window since the HUD keeps the count above zero forever), and the detail's `코크핏에서 열기` **navigates the main window, never this one** (`will-navigate` → `openInMainWindow`) — a 120px pill must never become a cockpit. A taken accelerator logs and offers the menu's alternatives; it never crashes the app. **Nothing drawn — zero new colors, zero new components** |
| Icon system | inline Lucide `<symbol>` sprite (`src/icons.ts` → `ICON_SPRITE`) + `.ic` class (`ICON_CSS`) | **real Lucide SVG paths** (ISC, `licenses/lucide.txt`) copied verbatim — **no CDN, no runtime dep** (the board is one HTML string under a strict CSP). A hidden `<svg style="position:absolute;width:0;height:0">` of `<symbol id="i-…" viewBox="0 0 24 24">` sits at the top of `<body>` in **both** `board.ts` and `login.ts`; usage is `<svg class="ic"><use href="#i-x"/></svg>` (static) or the client `ic('x')` helper (`ICON_JS_HELPER`, for dynamically-built HTML — sets `.innerHTML`, never `.textContent`). `.ic{width:1em;height:1em;stroke:currentColor;fill:none;…}` → **size follows font-size, color follows `currentColor`** so status classes (`.req.warn`, `.card.selected .selbox`, toast tints) recolor the icon automatically. Swapped: lock/unlock (login), terminal · folder · branch (`⎇`) · merge · play (Run) · check (done/select) · x (close) · refresh · recycle (Reclaim `♻`) · pencil (steer) · message (answer `◆`/review) · file (doc) · image (page/file) · code · chevron · chevron-down (model combo caret) · plus (`＋`, also count `+`) · minus (count `−`) · external-link (remote `🔗`/PR) · alert-triangle (missing deliverable) · bell/bell-off (notify). Left as text: prose arrows (`→`), dropdown carets (`▾`/`▸`), goal-band marks (`⌒`/`⌁`/`↳`), CSS status dots. Extend by adding one `<symbol>` (a real Lucide path) — never a font, never a CDN |

## Interaction rules

- **No native UI.** System dialogs/selects break the instrument feel and can't be themed. (Desktop-shell OS menus are the one exception — they should feel native to the OS.)
- Destructive = `confirmUI` with `danger: true` and an explicit consequence sentence. Never double-confirm.
- Feedback for every mutation: success → `ok` toast, failure → `error` toast carrying the server's `detail`.
- Focus is always visible (`:focus-visible` teal ring). Esc closes the topmost layer.
- Motion is functional only (pulse = running, flash = updated) and disabled under `prefers-reduced-motion`.
- Copy: short, lowercase-leaning, concrete ("worktree missing on machine", not "an error occurred").

## Extending

New UI goes through this file first: reuse a component; if none fits, add the component *here* (tokens + rules) in the same commit that introduces it. If the board ever splits into multiple pages or the Next.js front lands, these tokens move to a shared stylesheet unchanged.
