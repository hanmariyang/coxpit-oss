{
  "name": "coxpit",
  "version": "2.0.0-p1",
  "description": "Owner-first web cockpit to run and steer a fleet of AI coding agents across your own machines.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@fastify/websocket": "^11",
    "@libsql/client": "^0.14",
    "dotenv": "^16",
    "drizzle-orm": "^0.38",
    "fastify": "^5"
  },
  "devDependencies": {
    "@types/node": "^22",
    "drizzle-kit": "^0.30",
    "tsx": "^4",
    "typescript": "^5"
  }
}
