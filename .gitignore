node_modules/
dist/
*.db
*.db-shm
*.db-wal
.env
.DS_Store

# local dev notes (batch-fix backlog) — never publish
/BACKLOG.md

# local reference mocks (not part of the product)
scratchpad/

# machine-local claude shim (only appears when the data dir is the repo, i.e. a legacy ./coxpit.db)
/shim/
